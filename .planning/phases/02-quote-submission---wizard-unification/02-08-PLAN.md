---
phase: 02-quote-submission-wizard-unification
plan: 08
type: execute
wave: 4
depends_on: ["02-06", "02-07"]
files_modified:
  - Development/app/portal/admin/active-projects/[id]/page.tsx
  - Development/app/portal/admin/quotes/[id]/page.tsx
  - Development/components/admin/InspectionViewer.tsx
autonomous: true

must_haves:
  truths:
    - "Admin quote and project detail pages filter document categories by service"
    - "Inspection tab and viewer only appear for efficiency projects"
    - "Non-efficiency projects show an inspection-unavailable message instead of inspection UI"
  artifacts:
    - path: "Development/app/portal/admin/active-projects/[id]/page.tsx"
      provides: "Efficiency-only inspection tab guard"
      contains: ["inspection", "project.service"]
    - path: "Development/app/portal/admin/quotes/[id]/page.tsx"
      provides: "Service-aware DocumentManager usage"
      contains: ["DocumentManager", "serviceType"]
    - path: "Development/components/admin/InspectionViewer.tsx"
      provides: "Inspection UI guard for non-efficiency services"
      contains: ["serviceType"]
  key_links:
    - from: "Development/app/portal/admin/active-projects/[id]/page.tsx"
      to: "Development/components/DocumentManager.tsx"
      via: "passes serviceType from project.service"
      pattern: "<DocumentManager[\s\S]*serviceType"
    - from: "Development/app/portal/admin/quotes/[id]/page.tsx"
      to: "Development/components/DocumentManager.tsx"
      via: "passes serviceType from quote.service"
      pattern: "<DocumentManager[\s\S]*serviceType"
---

<objective>
Wire service-aware document filtering in admin pages and enforce efficiency-only inspection workflow.

Purpose: Admin pages should honor service-specific document categories and prevent inspection workflows for non-efficiency services (SVC-04).
Output: Admin pages pass serviceType to DocumentManager and hide inspection UI for consulting/advocacy.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-quote-submission---wizard-unification/02-CONTEXT.md
@.planning/phases/02-quote-submission---wizard-unification/02-06-SUMMARY.md
@.planning/phases/02-quote-submission---wizard-unification/02-07-SUMMARY.md
@Development/app/portal/admin/active-projects/[id]/page.tsx
@Development/app/portal/admin/quotes/[id]/page.tsx
@Development/components/admin/InspectionViewer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass serviceType into DocumentManager on admin pages</name>
  <files>
    Development/app/portal/admin/active-projects/[id]/page.tsx
    Development/app/portal/admin/quotes/[id]/page.tsx
  </files>
  <action>
**Update DocumentManager call sites to pass serviceType:**

1. **Admin active project detail page:**
   Update `Development/app/portal/admin/active-projects/[id]/page.tsx`:
   - Replace any hardcoded `allowedCategories` with `serviceType={project.service}`.
   - Keep `allowedCategories` ONLY if it must override defaults.

   ```tsx
   <DocumentManager
     entityType="active_projects"
     entityId={project.id!}
     title="Documents"
     serviceType={project.service}
   />
   ```

2. **Admin quote detail page:**
   Update `Development/app/portal/admin/quotes/[id]/page.tsx`:
   - Remove non-standard `allowedCategories` arrays (e.g., `quote`).
   - Pass `serviceType={quote.service}` (normalize if needed).

   ```tsx
   <DocumentManager
     entityType="quotes"
     entityId={quote.id}
     title={t.documentHub}
     serviceType={quote.service}
   />
   ```

3. **Type safety:**
   If `quote.service` or `project.service` can be undefined, guard with a fallback:
   ```tsx
   serviceType={(quote.service || 'consulting') as any}
   ```
  </action>
  <verify>
 1. Confirm both pages compile: `npx tsc --noEmit Development/app/portal/admin/active-projects/[id]/page.tsx Development/app/portal/admin/quotes/[id]/page.tsx`
 2. Inspect rendered DocumentManager props in React DevTools - serviceType is present.
  </verify>
  <done>
 - DocumentManager receives serviceType in both admin pages
 - Hardcoded allowedCategories removed unless truly needed
 - Category dropdown reflects SERVICE_DOCUMENT_CATEGORIES for each service
  </done>
</task>

<task type="auto">
  <name>Task 2: Restrict inspection workflow to efficiency projects</name>
  <files>
    Development/app/portal/admin/active-projects/[id]/page.tsx
    Development/components/admin/InspectionViewer.tsx
  </files>
  <action>
**Add efficiency-only guard for inspection UI (SVC-04):**

1. **Admin active project detail page:**
   - Add a guard `const isEfficiency = project?.service === 'efficiency';`.
   - Only render the Inspection tab button when `isEfficiency` is true.
   - If `activeTab === 'inspection'` and `!isEfficiency`, render a notice:
     "Inspection is only available for efficiency projects."

2. **InspectionViewer component:**
   - Add an optional `serviceType?: ServiceType` prop.
   - If `serviceType` is present and not `efficiency`, render a small info state and skip data loading.
   - Update the admin active project page to pass `serviceType={project.service}` to InspectionViewer.
  </action>
  <verify>
 1. Open a consulting or advocacy project → inspection tab is hidden and inspection UI does not render.
 2. Open an efficiency project → inspection tab appears and inspection UI works as before.
  </verify>
  <done>
 - Inspection tab only appears for efficiency projects
 - Non-efficiency projects show a clear inspection-unavailable message
 - InspectionViewer skips loading when service is not efficiency
  </done>
</task>

</tasks>

<verification>
1. **Document category filtering:**
   - Consulting quote/project: "Utility Bill" is not present
   - Advocacy quote/project: "Claim Evidence" and "Regulatory Filing" are present

2. **Inspection guard:**
   - Consulting/advocacy projects do not show inspection UI
   - Efficiency projects retain inspection access
</verification>

<success_criteria>
1. Admin DocumentManager instances pass serviceType
2. Inspection tab and viewer are limited to efficiency projects
3. Non-efficiency projects show an inspection-unavailable message
4. TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-quote-submission---wizard-unification/02-08-SUMMARY.md`
</output>
