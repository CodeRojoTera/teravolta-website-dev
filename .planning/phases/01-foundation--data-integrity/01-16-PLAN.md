---
phase: 01-foundation--data-integrity
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - app/portal/admin/users/staff/page.tsx
  - app/portal/admin/users/technicians/page.tsx
  - app/portal/admin/users/clients/page.tsx
  - app/portal/customer/settings/page.tsx
  - app/api/users/[id]/deletion/route.ts
  - lib/services/user-deletion.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Admin can schedule and cancel user deletions with a 15-day grace period"
    - "Customers can request deletion and cancel during the grace period"
    - "User deletion-driven project status changes go through the state machine"
    - "Legacy deletion_requests flow is not used in UI or API"
  artifacts:
    - path: "app/api/users/[id]/deletion/route.ts"
      provides: "Server-side deletion actions (schedule/cancel/hard)"
      exports: ["POST"]
    - path: "app/portal/admin/users/staff/page.tsx"
      provides: "Admin staff deletion UI wired to deletion API"
    - path: "app/portal/admin/users/technicians/page.tsx"
      provides: "Admin technician deletion UI wired to deletion API"
    - path: "app/portal/admin/users/clients/page.tsx"
      provides: "Admin client deletion UI wired to deletion API"
    - path: "app/portal/customer/settings/page.tsx"
      provides: "Customer deletion schedule/cancel UI"
    - path: "lib/services/user-deletion.ts"
      provides: "State machine status updates during deletion flows"
  key_links:
    - from: "app/portal/admin/users/staff/page.tsx"
      to: "/api/users/[id]/deletion"
      via: "fetch POST action schedule/cancel/hard"
      pattern: "fetch\(`/api/users/\$\{.*\}/deletion`"
    - from: "app/portal/admin/users/technicians/page.tsx"
      to: "/api/users/[id]/deletion"
      via: "fetch POST action schedule/cancel/hard"
      pattern: "fetch\(`/api/users/\$\{.*\}/deletion`"
    - from: "app/portal/customer/settings/page.tsx"
      to: "/api/users/[id]/deletion"
      via: "fetch POST action schedule/cancel"
      pattern: "fetch\(`/api/users/\$\{.*\}/deletion`"
    - from: "lib/services/user-deletion.ts"
      to: "lib/services/project-service.ts"
      via: "updateProjectStatus"
      pattern: "updateProjectStatus\("
---

<objective>
Ensure deletion workflows use the scheduling API and state machine so users can be recovered within the grace period.

Purpose: Close verification gaps in deletion UX and state-machine enforcement.
Output: Admin/customer deletion flows wired to the API with scheduling/cancel support.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation--data-integrity/01-04-SUMMARY.md
@.planning/phases/01-foundation--data-integrity/01-06-SUMMARY.md

@app/api/users/[id]/deletion/route.ts
@lib/services/user-deletion.ts
@app/portal/admin/users/clients/page.tsx
@app/portal/admin/users/staff/page.tsx
@app/portal/admin/users/technicians/page.tsx
@app/portal/customer/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route admin deletion actions through the deletion API</name>
  <files>app/portal/admin/users/staff/page.tsx, app/portal/admin/users/technicians/page.tsx, app/portal/admin/users/clients/page.tsx, app/api/users/[id]/deletion/route.ts</files>
  <action>
Replace any direct `supabase.from('users').update/delete` deletion handling with `/api/users/[id]/deletion` POST calls using `{ action: 'schedule' | 'cancel' | 'hard', reason }`.
Use `active_users.deletion_scheduled_for` to reflect scheduled state in list UI and show cancel controls for scheduled users.
Keep direct users table writes for profile edits only; do not reintroduce deletion_requests.
Ensure API route accepts schedule/cancel/hard actions and returns scheduledFor so the UI can update the table state.
  </action>
  <verify>Review the admin pages to confirm deletions call `/api/users/[id]/deletion` and no `deletion_requests` references remain.</verify>
  <done>Admin staff, technician, and client deletion actions schedule/cancel/hard delete via the API and show scheduled status.</done>
</task>

<task type="auto">
  <name>Task 2: Align customer deletion settings with scheduled deletion API</name>
  <files>app/portal/customer/settings/page.tsx</files>
  <action>
Ensure the customer settings page reads the current user profile via `active_users` for soft-delete filtering.
Route deletion request and cancellation through `/api/users/[id]/deletion` and use `deletion_scheduled_for` from `active_users` to drive the pending/cancel UI state.
Do not use deletion_requests or direct `users` deletes from the client.
  </action>
  <verify>Confirm `app/portal/customer/settings/page.tsx` reads from `active_users` and deletion actions call `/api/users/[id]/deletion`.</verify>
  <done>Customer settings reflect scheduled deletion state and allow cancel during the grace period.</done>
</task>

<task type="auto">
  <name>Task 3: Verify deletion service uses state machine for status updates</name>
  <files>lib/services/user-deletion.ts</files>
  <action>
Confirm that deletion scheduling and soft delete paths call `updateProjectStatus` for active project cancellations.
If any direct status updates remain, replace them with `updateProjectStatus` calls that include audit-friendly notes.
  </action>
  <verify>`npx tsc --noEmit lib/services/user-deletion.ts`</verify>
  <done>Deletion-driven project status changes go through the state machine service.</done>
</task>

</tasks>

<verification>
Admin and customer deletion flows schedule/cancel via API, and status changes route through the state machine.
</verification>

<success_criteria>
Admin and customer users can schedule and cancel deletion with the 15-day grace period; no UI uses deletion_requests; deletion-driven status changes use updateProjectStatus.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation--data-integrity/01-16-SUMMARY.md`
</output>
