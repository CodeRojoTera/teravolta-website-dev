---
phase: 01-foundation--data-integrity
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260130000004_create_deletion_audit_triggers.sql
  - supabase/migrations/20260130000021_cleanup_deletion_requests.sql
  - supabase/migrations/20260130000020_add_soft_delete_to_documents.sql
  - supabase/migrations/20260201000001_add_documents_soft_delete_columns.sql
  - supabase/migrations/20260201000002_add_notes_to_deletion_audit_log.sql
  - supabase/migrations/20260201000003_add_service_role_policy_deletion_audit_log.sql
  - lib/documentUtils.ts
  - components/DocumentList.tsx
  - components/DocumentManager.tsx
  - app/services/activeProjectService.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Deletion audit entries are created for deletes and soft deletes"
    - "Legacy deletion_requests table is removed and no longer referenced"
    - "Documents are soft-deleted and filtered out of reads"
  artifacts:
    - path: "supabase/migrations/20260130000004_create_deletion_audit_triggers.sql"
      provides: "Deletion audit trigger function and triggers"
      contains: "CREATE TRIGGER"
    - path: "supabase/migrations/20260130000021_cleanup_deletion_requests.sql"
      provides: "Legacy deletion_requests cleanup"
      contains: "DROP TABLE IF EXISTS public.deletion_requests"
    - path: "supabase/migrations/20260130000020_add_soft_delete_to_documents.sql"
      provides: "Documents soft delete columns"
      contains: "deleted_at"
    - path: "lib/documentUtils.ts"
      provides: "Soft delete updates and deleted_at filtered reads"
  key_links:
    - from: "supabase/migrations/20260130000004_create_deletion_audit_triggers.sql"
      to: "deletion_audit_log"
      via: "trigger insert"
      pattern: "INSERT INTO deletion_audit_log"
    - from: "lib/documentUtils.ts"
      to: "documents"
      via: "update deleted_at/deleted_by"
      pattern: "update\({\n\s*deleted_at"
---

<objective>
Apply database migrations for deletion auditing and ensure documents are soft-deleted and filtered throughout the UI and services.

Purpose: Close data integrity gaps from verification by applying pending migrations and confirming soft delete behavior.
Output: Deletion audit triggers applied, legacy deletion_requests removed, document reads filter deleted_at.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation--data-integrity/01-08-SUMMARY.md
@.planning/phases/01-foundation--data-integrity/01-09-SUMMARY.md

@supabase/migrations/20260130000004_create_deletion_audit_triggers.sql
@supabase/migrations/20260130000021_cleanup_deletion_requests.sql
@supabase/migrations/20260130000020_add_soft_delete_to_documents.sql
@lib/documentUtils.ts
@components/DocumentList.tsx
@components/DocumentManager.tsx
@app/services/activeProjectService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply deletion audit trigger migration and verify logging</name>
  <files>supabase/migrations/20260130000004_create_deletion_audit_triggers.sql, supabase/migrations/20260201000002_add_notes_to_deletion_audit_log.sql, supabase/migrations/20260201000003_add_service_role_policy_deletion_audit_log.sql</files>
  <action>
Apply the deletion audit trigger migration (and any dependent audit log policy/notes migrations) to the target Supabase database if not already applied.
Verify that deletes/soft deletes on users/documents create entries in `deletion_audit_log`.
If triggers are missing or failing, adjust the migration SQL to match the current schema and re-apply in a new migration.
  </action>
  <verify>Run a delete/soft-delete test and query `deletion_audit_log` to confirm a new entry exists for the affected record.</verify>
  <done>Deletion triggers are installed and audit entries are created on deletes.</done>
</task>

<task type="auto">
  <name>Task 2: Apply deletion_requests cleanup and remove any references</name>
  <files>supabase/migrations/20260130000021_cleanup_deletion_requests.sql</files>
  <action>
Apply the cleanup migration that migrates legacy deletion_requests data and drops the table.
Search the codebase for any remaining deletion_requests references and remove or replace them with deletion_audit_log or admin_inquiries as appropriate.
  </action>
  <verify>Confirm `deletion_requests` no longer exists in the database and no code references remain.</verify>
  <done>Legacy deletion_requests table is dropped and the app no longer references it.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm document soft delete columns and filtering</name>
  <files>supabase/migrations/20260130000020_add_soft_delete_to_documents.sql, supabase/migrations/20260201000001_add_documents_soft_delete_columns.sql, lib/documentUtils.ts, components/DocumentList.tsx, components/DocumentManager.tsx, app/services/activeProjectService.ts</files>
  <action>
Ensure documents have `deleted_at` and `deleted_by` columns by applying the soft delete migration if needed.
Audit document CRUD utilities and UI components to confirm deletes update `deleted_at`/`deleted_by` and document reads filter `deleted_at IS NULL`.
If any document queries still hard-delete or omit the deleted_at filter, update them to follow the soft delete pattern.
  </action>
  <verify>Search for document deletes (should be update-only) and confirm document reads include `is('deleted_at', null)`.</verify>
  <done>Documents soft delete and all reads exclude deleted rows.</done>
</task>

</tasks>

<verification>
Deletion audit triggers log deletes, deletion_requests is dropped, and document reads exclude soft-deleted rows.
</verification>

<success_criteria>
Audit log contains entries for deletes/soft deletes, legacy deletion_requests is removed, and document CRUD follows soft delete conventions end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation--data-integrity/01-17-SUMMARY.md`
</output>
