---
phase: 02-quote-submission-wizard-unification
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - app/quote/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "When residential property selected with inspection checkbox UNCHECKED, form skips Schedule Inspection step"
    - "Step 4 only displays when inspection is requested OR commercial property type selected"
  artifacts:
    - path: "app/quote/page.tsx"
      provides: "Inspection step conditional logic checking inspectionRequested flag"
      contains: "currentStep === 4.*inspectionRequested"
  key_links:
    - from: "Inspection checkbox"
      to: "Step 4 rendering condition"
      via: "inspectionRequested state"
      pattern: "inspectionRequested.*currentStep === 4"
    - from: "Property type selection"
      to: "Step 4 visibility"
      via: "INSPECTION_REQUIRED_TYPES check"
      pattern: "INSPECTION_REQUIRED_TYPES"
---

<objective>
Fix inspection scheduling step logic in public quote form.

Current behavior: Residential property with inspection checkbox UNCHECKED still shows "Schedule Your Inspection" step.
Expected behavior: Skip Step 4 entirely if residential/apartment/small-business AND inspection NOT requested.

Purpose: Respects user's choice to skip inspection for eligible property types, improves form UX by removing unnecessary steps.

Output: Quote form conditional rendering includes inspectionRequested flag check
</objective>

<execution_context>
@./.Claude/get-shit-done/workflows/execute-plan.md
@./.Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-quote-submission---wizard-unification/02-03-SUMMARY.md

# Key files
Development/Development/app/quote/page.tsx
Development/Development/lib/schemas/constants.ts (INSPECTION_REQUIRED_TYPES constant)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inspectionRequested check to Step 4 condition</name>
  <files>app/quote/page.tsx</files>
  <action>
    Find Step 4 conditional rendering around line 872:
    
    Current code:
    ```jsx
    {/* Step 4 - Booking (efficiency only) */}
    {currentStep === 4 && service === 'efficiency' && (
    ```
    
    This condition only checks service type. Add inspectionRequested check:
    ```jsx
    {/* Step 4 - Booking (efficiency only, if inspection requested) */}
    {currentStep === 4 && service === 'efficiency' && watchedInspectionRequested && (
    ```
    
    Why: For efficiency service, Step 4 (booking) should only show when:
    1. Current step is 4 (user navigated here)
    2. Service is efficiency
    3. Inspection was requested (either commercial property OR user checked inspection box)
    
    Note: `watchedInspectionRequested` already exists in form setup - it's the value of inspectionRequested checkbox from form.watch().
    
    If watchedInspectionRequested is not available in scope, check the form setup (around line 440-480) to see how form state is accessed, then use appropriate variable name.
  </action>
  <verify>
    1. Open app/quote/page.tsx line 872
    2. Confirm condition includes inspectionRequested check
    3. Test in browser:
       - Go to /quote form
       - Select Efficiency service
       - Select Residential property type (optional inspection property)
       - Proceed through steps
       - At inspection notice step, DO NOT check "Schedule inspection" checkbox
       - Proceed forward
       - Step 4 (booking) should NOT appear
       - Form should complete with fewer steps
    4. Verify with Residential + checked inspection box: Step 4 SHOULD appear
  </verify>
  <done>Step 4 condition includes inspectionRequested check - skips step when unchecked for eligible properties</done>
</task>

</tasks>

<verification>
After task completes:

1. **Functional test** — Public quote form behavior:
   - Scenario A: Efficiency + Residential + inspection UNCHECKED
     → Should skip Step 4, proceed to final submission
   - Scenario B: Efficiency + Residential + inspection CHECKED
     → Should show Step 4 with booking date/time
   - Scenario C: Efficiency + Hotel (commercial)
     → Should show Step 4 regardless of checkbox (commercial requires inspection)

2. **Code verification**:
   - Line 872 condition includes `&& watchedInspectionRequested` or equivalent
   - npm run build completes without errors
</verification>

<success_criteria>
✓ Step 4 condition checks inspectionRequested state
✓ Residential + inspection unchecked = skips Step 4
✓ Residential + inspection checked = shows Step 4
✓ Commercial properties (hotel/building/industrial) always show Step 4
✓ npm run build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02-quote-submission---wizard-unification/02-10-SUMMARY.md`

Include:
- Condition change made to Step 4 rendering
- Test scenarios and results (all 3 scenarios verified)
- Behavior confirmation (step skip working as expected)
</output>
