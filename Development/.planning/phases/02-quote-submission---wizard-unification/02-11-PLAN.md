---
phase: 02-quote-submission-wizard-unification
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ManualProjectWizard.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Address field shows 'Installation Address' label for efficiency service"
    - "Address field shows 'Address' label for consulting and advocacy services"
  artifacts:
    - path: "components/ManualProjectWizard.tsx"
      provides: "Conditional address label based on service type"
      contains: "service.*efficiency.*Installation Address"
  key_links:
    - from: "Service type selection"
      to: "Address field rendering"
      via: "conditional label"
      pattern: "formData\\.project\\.service.*address.*label"
---

<objective>
Fix address field label in admin manual project wizard to be service-specific.

Current behavior: All services show "Installation Address" label (efficiency-specific term).
Expected behavior: Efficiency shows "Installation Address", consulting/advocacy show "Address".

Purpose: Improves clarity by using appropriate terminology for each service type (installation is efficiency-specific, consulting/advocacy don't use that term).

Output: Address field label conditional on service type
</objective>

<execution_context>
@./.Claude/get-shit-done/workflows/execute-plan.md
@./.Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-quote-submission---wizard-unification/02-05-SUMMARY.md
@.planning/phases/02-quote-submission---wizard-unification/02-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make address label conditional on service type in text strings</name>
  <files>components/ManualProjectWizard.tsx</files>
  <action>
    Find the text strings object around line 260 that contains "Installation Address".
    
    Current code (in both en and es text objects):
    ```
    address: 'Installation Address',
    ```
    
    Replace with SERVICE-TYPE-SPECIFIC versions. This means you need to:
    
    1. Remove the single hardcoded `address: 'Installation Address'` entry
    2. Create TWO conditional entries instead:
       - `addressEfficiency: 'Installation Address'` (for efficiency service)
       - `addressOther: 'Address'` (for consulting/advocacy)
    
    Apply this to BOTH English (en) and Spanish (es) text objects:
    - English: 'Installation Address' and 'Address'
    - Spanish: 'Dirección de Instalación' and 'Dirección'
    
    Example structure:
    ```
    en: {
      ...
      addressEfficiency: 'Installation Address',
      addressOther: 'Address',
      ...
    },
    es: {
      ...
      addressEfficiency: 'Dirección de Instalación',
      addressOther: 'Dirección',
      ...
    }
    ```
  </action>
  <verify>
    1. Open components/ManualProjectWizard.tsx
    2. Confirm around line 260:
       - `addressEfficiency: 'Installation Address'` exists in en object
       - `addressOther: 'Address'` exists in en object
       - Same Spanish equivalents exist in es object
    3. No single `address:` entry remains
  </verify>
  <done>Text strings include both addressEfficiency and addressOther for both languages</done>
</task>

<task type="auto">
  <name>Task 2: Update address field rendering to use conditional label</name>
  <files>components/ManualProjectWizard.tsx</files>
  <action>
    Find the address field rendering around line 464 where the label is displayed.
    
    Current code:
    ```jsx
    <label className="block text-sm font-medium text-gray-700 mb-1">{t.address}</label>
    ```
    
    Replace with conditional logic:
    ```jsx
    <label className="block text-sm font-medium text-gray-700 mb-1">
      {formData.project.service === 'efficiency' ? t.addressEfficiency : t.addressOther}
    </label>
    ```
    
    Why: This checks the current service type and shows appropriate label:
    - Efficiency → "Installation Address"
    - Consulting/Advocacy → "Address"
    
    Ensure formData.project.service is the correct path to access current service type (confirm by checking other conditional renders in the same component).
  </action>
  <verify>
    1. Open components/ManualProjectWizard.tsx line ~464
    2. Confirm address label uses ternary conditional on service type
    3. Test in browser (/admin/projects/manual):
       - Create efficiency project → address shows "Installation Address"
       - Create consulting project → address shows "Address"
       - Create advocacy project → address shows "Address"
  </verify>
  <done>Address field label conditional on service type in rendering</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Visual verification** — Admin manual project wizard:
   - Open http://localhost:3000/admin/projects/manual (or relevant admin URL)
   - Efficiency service: Confirm address label says "Installation Address"
   - Consulting service: Confirm address label says "Address"
   - Advocacy service: Confirm address label says "Address"
   
2. **Spanish verification**:
   - Switch language to Spanish
   - Repeat visual checks with Spanish labels

3. **Code verification**:
   - npm run build completes without errors
</verification>

<success_criteria>
✓ Efficiency projects show "Installation Address" label
✓ Consulting projects show "Address" label
✓ Advocacy projects show "Address" label
✓ Spanish translations work correctly
✓ npm run build completes successfully
✓ No console errors on admin manual project page
</success_criteria>

<output>
After completion, create `.planning/phases/02-quote-submission---wizard-unification/02-11-SUMMARY.md`

Include:
- Text string changes (addressEfficiency, addressOther added)
- Rendering logic update (conditional ternary operator)
- Visual test results (labels display correctly per service type)
- Language verification (Spanish labels working)
</output>
