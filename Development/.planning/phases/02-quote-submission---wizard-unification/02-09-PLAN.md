---
phase: 02-quote-submission-wizard-unification
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/schemas/constants.ts
  - app/quote/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Industrial property type card displays an icon matching other property types"
    - "Time picker input accepts only hourly increments (step=3600)"
  artifacts:
    - path: "lib/schemas/constants.ts"
      provides: "PROPERTY_TYPE_LABELS with valid ri-factory-line replacement"
      contains: "industrial.*icon.*ri-"
    - path: "app/quote/page.tsx"
      provides: "Time input with step=3600 constraint"
      contains: 'step="3600"'
  key_links:
    - from: "PropertyTypeSelector.tsx"
      to: "PROPERTY_TYPE_LABELS[type].icon"
      via: "label rendering"
      pattern: "labels\\.icon"
    - from: "Quote step 4"
      to: "HTML time input"
      via: "input element attribute"
      pattern: 'type="time".*step='
---

<objective>
Fix two minor UI issues in the public quote form:

1. **Industrial property icon missing** — The industrial property type card shows no icon (others have icons). Root cause: `ri-factory-line` doesn't exist in Remix Icons library. Fix: Replace with `ri-building-4-line` or equivalent.

2. **Time picker allows minute-level selection** — Time input allows selecting minutes (e.g., 2:45 PM) instead of hour-level only (e.g., 2:00 PM). Root cause: Missing `step="3600"` attribute on time input. Fix: Add step attribute to constrain to hourly increments.

Purpose: Industrial icon ensures visual consistency with other property types. Hour-level time picker provides cleaner UX and reduces data noise.

Output: Quote form displays all 6 property types with icons, time picker constrains to hours
</objective>

<execution_context>
@./.Claude/get-shit-done/workflows/execute-plan.md
@./.Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-quote-submission---wizard-unification/02-01-SUMMARY.md
@.planning/phases/02-quote-submission---wizard-unification/02-03-SUMMARY.md

# Key implementation files
Development/Development/lib/schemas/constants.ts
Development/Development/app/quote/page.tsx
Development/Development/components/wizards/shared/PropertyTypeSelector.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace industrial icon with ri-building-4-line</name>
  <files>lib/schemas/constants.ts</files>
  <action>
    In PROPERTY_TYPE_LABELS object, find the industrial property type icon entry (currently set to 'ri-factory-line').
    
    Replace:
    ```
    industrial: {
      en: 'Industrial',
      es: 'Industrial',
      icon: 'ri-factory-line',
    },
    ```
    
    With:
    ```
    industrial: {
      en: 'Industrial',
      es: 'Industrial',
      icon: 'ri-building-4-line',
    },
    ```
    
    Reason: ri-factory-line doesn't exist in current Remix Icons version. ri-building-4-line is available and semantically appropriate for industrial buildings.
  </action>
  <verify>
    1. Open lib/schemas/constants.ts
    2. Confirm industrial.icon = 'ri-building-4-line'
    3. Run: npm run build (should complete without errors)
  </verify>
  <done>industrial icon in constants.ts changed to ri-building-4-line</done>
</task>

<task type="auto">
  <name>Task 2: Add step="3600" to time input for hourly-only selection</name>
  <files>app/quote/page.tsx</files>
  <action>
    Find the time input field in Step 4 (Booking) - around line 895.
    
    Current code:
    ```jsx
    <input
      type="time"
      {...register('bookingTime' as any)}
      className="w-full p-4 border border-gray-300 rounded-lg text-lg focus:ring-2 focus:ring-[#004a90] outline-none"
    />
    ```
    
    Update to add step attribute:
    ```jsx
    <input
      type="time"
      step="3600"
      {...register('bookingTime' as any)}
      className="w-full p-4 border border-gray-300 rounded-lg text-lg focus:ring-2 focus:ring-[#004a90] outline-none"
    />
    ```
    
    Reason: HTML time input step attribute in seconds. 3600 seconds = 1 hour, constrains picker to hourly increments.
  </action>
  <verify>
    1. Open app/quote/page.tsx, find step 4 time input (line ~895)
    2. Confirm: step="3600" attribute exists on time input
    3. Test in browser: Go to quote form, select efficiency service, reach step 4 (booking), click time field - should show only hour options (no minute granularity)
  </verify>
  <done>Time input includes step="3600" to constrain to hourly increments</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Visual verification** — Open public quote page (/quote):
   - Select Efficiency service
   - Proceed through steps 1-3
   - Reach Step 4 (Schedule Your Inspection)
   - Verify: Time picker shows only hour options, no minutes
   - Go back to Step 1, check all 6 property type cards including Industrial - all should show icons

2. **Code verification** — Run build:
   ```bash
   npm run build
   ```
   Should complete without TypeScript errors related to these changes.
</verification>

<success_criteria>
✓ Industrial property type card displays ri-building-4-line icon (visible in quote form)
✓ Time picker in Step 4 constrains to hourly increments (step="3600" attribute present)
✓ npm run build completes successfully
✓ Quote form renders without console errors related to property types or time input
</success_criteria>

<output>
After completion, create `.planning/phases/02-quote-submission---wizard-unification/02-09-SUMMARY.md`

Include:
- Changes made to constants.ts (icon replacement)
- Changes made to quote/page.tsx (step attribute)
- Visual test results (time picker behavior, icon display)
</output>
