---
phase: 02-quote-submission-wizard-unification
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - Development/app/quote/page.tsx
  - Development/components/ManualProjectWizard.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Industrial property type card displays icon like other property type options"
    - "Time picker shows hour-level granularity only (no minute selection)"
    - "Residential property with unchecked inspection skips inspection scheduling step"
    - "Address field label is service-specific: 'Installation Address' for efficiency, 'Address' for consulting/advocacy"
  artifacts:
    - path: "Development/app/quote/page.tsx"
      provides: "Quote form with 6 property types (industrial icon included), time picker constraints, conditional inspection step"
      min_lines: 100
    - path: "Development/components/ManualProjectWizard.tsx"
      provides: "Service-aware address field label rendering"
      min_lines: 50
  key_links:
    - from: "app/quote/page.tsx property type selector"
      to: "industrial icon rendering"
      via: "ri-factory-line icon in option.icon"
      pattern: "industrial.*ri-factory-line"
    - from: "app/quote/page.tsx time input"
      to: "hour-only constraint"
      via: "type=time step attribute"
      pattern: "type.*time.*step.*3600"
    - from: "app/quote/page.tsx step 2 to 4 navigation"
      to: "inspectionRequested state check"
      via: "conditional step logic"
      pattern: "inspectionRequested|currentStep.*4"
---

# Phase 02 Plans 12-14: Gap Closure - UAT Fixes

## Objective

Fix four UAT issues identified in Phase 02 verification:
1. **Minor**: Industrial property type card missing icon (Test 1)
2. **Major**: Time picker allows minute-level instead of hour-level (Test 3)
3. **Major**: Residential with unchecked inspection still shows inspection step (Test 4)
4. **Minor**: Address label not conditional on service type (Test 11)

All fixes are UI/UX and will be combined into 2-3 focused plans.

Purpose: Get Phase 02 UAT-verified without blocking quote submission flow
Output: Updated components with all 4 issues resolved

## Execution Context

@./.Claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/02-quote-submission---wizard-unification/02-11-SUMMARY.md

## Context

@.planning/STATE.md (Phase 02 complete, gap closure needed)
@.planning/phases/02-quote-submission---wizard-unification/02-UAT.md (gap definitions)

## Tasks

<task type="auto">
  <name>Task 1: Fix Industrial Icon + Time Picker Constraints in Quote Form</name>
  <files>Development/app/quote/page.tsx</files>
  <action>
  Two UI fixes in app/quote/page.tsx:

  **Fix 1: Industrial Icon (Line 664)**
  - Current: property type selector has 6 options but industrial icon is missing
  - Issue: Icon is already defined as `ri-factory-line` but may not be rendering correctly
  - Action: Verify line 664 has the industrial icon rendering correctly. If icon is not showing, check:
    * CSS classes are applied correctly to icon element
    * Icon library (Remix Icon) is loaded and `ri-factory-line` is valid
    * No CSS that hides the icon

  **Fix 2: Time Picker Hour-Level Granularity (Line 798)**
  - Current: `<input type="time" name="bookingTime" ...>` allows minute selection
  - Action: Add step attribute to constrain to hour-level only
    * Change: `<input type="time" ... />` to `<input type="time" step="3600" ... />`
    * step="3600" = 1 hour in seconds (60 min × 60 sec)
    * Result: Time picker will only show hour selections (00:00, 01:00, 02:00, etc.)
  - Verify: After saving, HTML time input should have step="3600" attribute
  </action>
  <verify>
  1. Open Development/app/quote/page.tsx and locate line 664 (property type selector for industrial)
  2. Verify: `<i className={`${option.icon} text-xl md:text-2xl text-[#004a90]`}</i>` renders icon for all 6 property types
  3. In browser: Visit /quote, select efficiency service, Step 2: Industrial property card should show factory icon (same color/size as other icons)
  4. Step 4: Time picker should only allow hour selection (no minute increments) - spinner/input should jump by 1-hour increments
  5. Both files saved with changes applied
  </verify>
  <done>
  - Industrial property type card displays matching icon like residential/apartment/hotel/building/small-business
  - Time picker step attribute set to 3600 seconds (1 hour granularity)
  - No other quote form behavior changed
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Inspection Request Flag + Conditional Step Rendering</name>
  <files>Development/app/quote/page.tsx</files>
  <action>
  Enable conditional skipping of inspection scheduling step based on property type and user choice.

  **Current Issue:** Step 4 (Schedule Your Inspection) always shows for efficiency service, even when property is residential and user didn't request inspection.

  **Required Logic:**
  - For efficiency service only:
    * Commercial property (hotel, building, industrial) = inspection required (show Step 4)
    * Residential/apartment/small-business = inspection optional
      - Add checkbox in Step 2: "Schedule inspection?" (checked by default)
      - If unchecked: Skip Step 4, go directly to confirmation
      - If checked: Show Step 4

  **Implementation:**
  1. Add state: `const [inspectionRequested, setInspectionRequested] = useState(true)`
  2. Add checkbox to Step 2 (after property size field, before Continue button):
     ```jsx
     {formData.service === 'efficiency' && !isCommercial() && (
       <label className="flex items-center gap-2">
         <input 
           type="checkbox" 
           checked={inspectionRequested}
           onChange={(e) => setInspectionRequested(e.target.checked)}
         />
         <span>{getText('scheduleInspection')}</span>
       </label>
     )}
     ```
  3. Update navigation logic in handleBillsContinue:
     - If efficiency AND residential AND inspectionRequested=false: Go to confirmation page
     - Otherwise: Go to Step 4 (inspection booking)
  4. Update step counter/progress to skip Step 4 when not needed

  **Translation Keys to Add:**
  - en: "Schedule inspection?"
  - es: "¿Agendar inspección?"

  This allows residential customers to skip inspection scheduling if not needed, unblocking quote submission.
  </action>
  <verify>
  1. In browser: Visit /quote, select efficiency service
  2. Step 2: Residential property selected
     - New checkbox appears: "Schedule inspection?"
     - Checkbox is checked by default
  3. Uncheck the inspection checkbox and click Continue
     - Should skip Step 3 (bills) and Step 4 (inspection)
     - Should go to confirmation/submission
  4. Checking the checkbox and clicking Continue should show Step 3 normally
  5. Commercial properties (hotel/building/industrial) should NOT show checkbox (inspection always required)
  6. Checking "Schedule inspection?" then clicking Continue should show Step 3 → Step 4 flow normally
  </verify>
  <done>
  - Inspection request checkbox appears for residential properties when service=efficiency
  - Unchecking inspection checkbox skips Step 4 (inspection scheduling)
  - Commercial properties still require inspection (no checkbox, Step 4 always shown)
  - Consulting/advocacy services unaffected (no inspection step shown)
  </done>
</task>

<task type="auto">
  <name>Task 3: Make Address Field Label Service-Specific in Admin Wizard</name>
  <files>Development/components/ManualProjectWizard.tsx</files>
  <action>
  Conditionally render address field label based on service type.

  **Current Issue (Line 440):** Address field shows "Installation Address" label for all services, but should be:
  - "Installation Address" for efficiency service only
  - "Address" for consulting and advocacy services

  **Implementation:**
  1. Locate line 440 where address label is rendered
  2. Replace hardcoded `{t.address}` with conditional logic:
     ```jsx
     <label className="block text-sm font-medium text-gray-700 mb-1">
       {formData.project.service === 'efficiency' 
         ? 'Installation Address' 
         : 'Address'}
     </label>
     ```
  3. Alternative if using translation keys: Update translation object to have both keys and use same conditional

  **Note:** This is purely a label change. The address field itself, validation, and data handling remain unchanged.
  </action>
  <verify>
  1. In admin portal > Create Manual Project > Step 2 (Project Details)
  2. Select Efficiency service: Address label shows "Installation Address"
  3. Select Consulting service: Address label shows "Address"
  4. Select Advocacy service: Address label shows "Address"
  5. Address input field behavior unchanged - no functional changes
  </verify>
  <done>
  - Address label is conditional on service type
  - Efficiency shows "Installation Address"
  - Consulting/advocacy show "Address"
  - No other admin wizard behavior affected
  </done>
</task>

</tasks>

## Verification

After completing all tasks, verify in browser:

1. **Quote Form Property Types**: Visit /quote → efficiency → step 2 → all 6 property types have icons (including industrial factory icon)
2. **Time Picker**: Step 4 → time input only increments by hours, not minutes
3. **Inspection Conditional**: 
   - Residential with inspection checked: shows all 4 steps
   - Residential with inspection unchecked: skips inspection step
   - Commercial: no inspection checkbox, always shows inspection step
4. **Admin Wizard Labels**: 
   - Efficiency service: "Installation Address"
   - Consulting/advocacy: "Address"

## Success Criteria

- Industrial icon renders correctly on property type selector
- Time picker constrained to hour-level selection
- Inspection step skippable for residential properties when not requested
- Address label service-specific in admin wizard
- All changes deployed to Development folder
- Phase 02 UAT gap issues closed

## Output

After completion, update `.planning/phases/02-quote-submission---wizard-unification/02-UAT.md`:
- Mark all 4 gaps as "resolved"
- Record completion time
- Note: RLS policy issue (2a) is Phase 1 blocker, deferred to Phase 1 gap closure
