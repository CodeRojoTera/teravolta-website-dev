---
phase: 02.1-antigravity-docs-audit-rebuild
plan: 10
type: execute
wave: 3
depends_on: ["04", "09"]
files_modified:
  - Development/docs/audits/antigravity-2026-02/INVENTORY.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Inventory lists all markdown files repo-wide (excluding .claude/.opencode/.gemini) with status and checklist"
    - "Inventory includes Plan04 Batch A and Plan09 scan-hit lists with remediation status"
    - "Legacy task record path is recorded accurately in the inventory"
  artifacts:
    - path: "Development/docs/audits/antigravity-2026-02/INVENTORY.md"
      provides: "Repo-wide markdown inventory with scan-hit routing sections"
      contains: "Markdown scan hits - Unassigned (Plan09)"
  key_links:
    - from: "Development/docs/audits/antigravity-2026-02/INVENTORY.md"
      to: "repo markdown list"
      via: "inventory count"
      pattern: "Total markdown files"
    - from: "Development/docs/audits/antigravity-2026-02/INVENTORY.md"
      to: "Code comment hits - Batch A (Plan04)"
      via: "section header"
      pattern: "Code comment hits - Batch A \(Plan04\)"
---

<objective>
Close inventory gaps by rebuilding the repo-wide markdown list and restoring scan-hit routing sections.

Purpose: Ensure the audit inventory is complete, count-accurate, and contains the required Plan04/Plan09 remediation lists.
Output: Updated inventory table with all markdown files and explicit scan-hit sections.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-antigravity-docs-audit-rebuild/02.1-CONTEXT.md
@.planning/phases/02.1-antigravity-docs-audit-rebuild/02.1-RESEARCH.md
@.planning/phases/02.1-antigravity-docs-audit-rebuild/02.1-01-PLAN.md
@Development/docs/audits/antigravity-2026-02/INVENTORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild repo-wide markdown inventory with accurate counts</name>
  <files>Development/docs/audits/antigravity-2026-02/INVENTORY.md</files>
  <action>
    Enumerate all .md files repo-wide while excluding .claude/, .opencode/, and .gemini/ directories.
    Use a script to generate a normalized, repo-relative path list (no leading ./) and compare against the existing inventory table.
    Update INVENTORY.md to include every missing markdown file as a new row with:
    - Scope (Full/Static/Legacy/GSD tooling) consistent with existing entries.
    - Status set to "Needs Review" for new rows unless the file was previously reviewed.
    - Checklist columns populated (Fixes Applied: "- [x] None"; Pending: "- [ ] Needs review: not yet audited").
    Preserve existing rows and their statuses; only add missing files and keep ordering stable (existing rows first, new rows appended).
    Add an explicit inventory row for `.gemini/` labeled "Out of Scope — GSD required" with scope "GSD tooling" while keeping `.gemini/` excluded from scans.
    Add a line near the top of INVENTORY.md with the total markdown file count (e.g., "Total markdown files: N") that excludes .claude/.opencode/.gemini, and ensure the table row count equals that count plus the single out-of-scope .gemini/ row.
  </action>
  <verify>Inventory row count equals (repo-wide markdown file count excluding .claude/.opencode/.gemini) plus the single .gemini/ out-of-scope row.</verify>
  <done>INVENTORY.md lists every repo markdown file with scope, status, and checklist entries.</done>
</task>

<task type="auto">
  <name>Task 2: Restore Plan04/Plan09 scan-hit sections with remediation status</name>
  <files>Development/docs/audits/antigravity-2026-02/INVENTORY.md</files>
  <action>
    Re-run the legacy term scan described in 02.1-01-PLAN.md across .md, .txt, and code comments (excluding .claude/.opencode/.gemini).
    Populate the required explicit lists under a "Scan Hits" section in INVENTORY.md:
    - "Markdown scan hits - Unassigned (Plan09)" (all scan-hit .md files not in Plans 02-07 file lists)
    - "Text scan hits (.txt) - Plan07"
    - "Code comment hits - Batch A (Plan04)" (limit to 8 files as stated in 02.1-01-SUMMARY.md)
    - "Code comment hits - Batch B (Plan09)" (all remaining comment hits)
    For each listed file, add a remediation status note (Updated/Legacy/Needs Review) based on the current content.
    Ensure the task record entry uses the correct repo-relative path that matches the actual file location.
  </action>
  <verify>INVENTORY.md contains all four scan-hit section headers with explicit file lists and remediation statuses.</verify>
  <done>Plan04/Plan09 scan-hit lists are present and each file shows its remediation status.</done>
</task>

</tasks>

<verification>
- Inventory count and table rows match the repo-wide markdown file list.
- Plan04/Plan09 scan-hit sections exist with explicit file lists and status notes.
</verification>

<success_criteria>
- Inventory is complete, count-accurate, and includes required scan-hit routing lists.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-antigravity-docs-audit-rebuild/02.1-10-SUMMARY.md`
</output>
