---
phase: 03-shared-service-features
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Development/app/portal/admin/active-projects/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Admin project detail shows consulting sections only for consulting projects"
    - "Admin project detail shows advocacy claim/evidence sections only for advocacy projects"
    - "Documents on admin project detail use service-specific categories"
  artifacts:
    - path: "Development/app/portal/admin/active-projects/[id]/page.tsx"
      provides: "Service-specific admin project detail sections"
      contains: "project.service"
  key_links:
    - from: "Development/app/portal/admin/active-projects/[id]/page.tsx"
      to: "Development/components/DocumentManager.tsx"
      via: "serviceType prop"
      pattern: "serviceType={project.service}"
---

<objective>
Make admin project detail service-specific and align document categories.

Purpose: Give admins a clear, service-tailored view of each project and ensure document categories match the service type.
Output: Updated admin project detail page with consulting/advocacy sections and service-aware documents.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shared-service-features/03-RESEARCH.md
@Development/app/portal/admin/active-projects/[id]/page.tsx
@Development/components/DocumentManager.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make admin document management service-aware</name>
  <files>Development/app/portal/admin/active-projects/[id]/page.tsx</files>
  <action>
Update the Documents card to use service-aware categories.

- Replace the hardcoded `allowedCategories` list with `serviceType={project.service}` on `DocumentManager`.
- Keep `allowedCategories` only when you want to narrow further (e.g., evidence/regulatory sections in advocacy), but the main documents section should rely on serviceType.
- Preserve existing layout and styling.
  </action>
  <verify>
Run:
```
rg "DocumentManager.*serviceType" Development/app/portal/admin/active-projects/[id]/page.tsx
```
Expect the main documents section to pass `serviceType`.
  </verify>
  <done>Admin project documents use service-specific categories via DocumentManager.</done>
</task>

<task type="auto">
  <name>Task 2: Add service-specific sections for consulting and advocacy</name>
  <files>Development/app/portal/admin/active-projects/[id]/page.tsx</files>
  <action>
Render project detail sections based on `project.service`.

- Consulting projects:
  - Add a "Consulting Overview" card showing timeline, budget, and phase summary (counts + total amount).
  - Keep the existing phases editor visible for consulting (no change to editing logic).
- Advocacy projects:
  - Add a "Claim Details" card showing claim type, distributor company, claim amount, incident date, and damage description (display "-" when missing).
  - Add separate "Evidence" and "Regulatory Filing" document sections that narrow categories to `claim_evidence` and `regulatory_filing`.
- Efficiency projects:
  - Keep the inspection tab and do not show consulting/advocacy sections.
- Ensure sections are clearly labeled, consistent with existing card styling, and only render for the appropriate service type.
  </action>
  <verify>
Run:
```
rg "Consulting Overview|Claim Details|claim_evidence|regulatory_filing" Development/app/portal/admin/active-projects/[id]/page.tsx
```
Expect the service-specific sections and category filters to be present.
  </verify>
  <done>Admin project detail displays consulting and advocacy sections only for their respective services.</done>
</task>

</tasks>

<verification>
- Consulting projects show timeline/budget/phases summary
- Advocacy projects show claim details and evidence/regulatory document sections
- Efficiency projects continue to show inspection tab only
</verification>

<success_criteria>
- Admin project detail is clearly service-specific and avoids irrelevant sections
- Document categories align with the service type in admin views
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-service-features/03-03-SUMMARY.md`
</output>
