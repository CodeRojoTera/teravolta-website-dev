---
phase: 03-shared-service-features
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Development/components/DocumentList.tsx
  - Development/app/portal/customer/projects/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Customer portal shows consulting-specific sections for consulting projects"
    - "Consulting customers can review project deliverables in the customer portal"
    - "Customer portal shows advocacy-specific sections for advocacy projects"
    - "Document lists can be filtered to service-specific categories"
  artifacts:
    - path: "Development/components/DocumentList.tsx"
      provides: "Allowed category filtering for document lists"
      contains: "allowedCategories"
    - path: "Development/app/portal/customer/projects/[id]/page.tsx"
      provides: "Service-specific customer project detail sections, including consulting deliverables"
      contains: "Deliverables"
  key_links:
    - from: "Development/app/portal/customer/projects/[id]/page.tsx"
      to: "Development/components/DocumentList.tsx"
      via: "allowedCategories prop"
      pattern: "allowedCategories"
---

<objective>
Add service-specific customer portal views for consulting and advocacy.

Purpose: Customers should see project details that match their service type and only relevant documents.
Output: Enhanced customer project detail view and document list filtering support.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shared-service-features/03-RESEARCH.md
@Development/app/portal/customer/projects/[id]/page.tsx
@Development/components/DocumentList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add allowed category filtering to DocumentList</name>
  <files>Development/components/DocumentList.tsx</files>
  <action>
Introduce an optional `allowedCategories` prop to filter documents and filter dropdown options.

- Add `allowedCategories?: string[]` to `DocumentListProps`.
- When provided, filter `documents` to only include categories in the list before applying `filterCategory`.
- Limit filter dropdown options to `allowedCategories` and add labels for `claim_evidence`, `regulatory_filing`, and `rfp`.
- Preserve existing behavior when `allowedCategories` is undefined.
- Update category badge colors to include the new advocacy categories and rfp.
  </action>
  <verify>
Run:
```
rg "allowedCategories|claim_evidence|regulatory_filing|rfp" Development/components/DocumentList.tsx
```
Expect the new prop and category labels to be present.
  </verify>
  <done>DocumentList supports service-specific category filtering without breaking existing views.</done>
</task>

<task type="auto">
  <name>Task 2: Add consulting and advocacy sections to customer project detail</name>
  <files>Development/app/portal/customer/projects/[id]/page.tsx</files>
  <action>
Render service-specific sections based on `project.service`.

- Consulting projects:
  - Add a "Consulting Overview" section showing timeline, budget, and phase summary (counts + total amount).
  - Reuse existing phases table for consulting projects and ensure it is shown only for consulting/advocacy.
  - Add a "Deliverables" section that uses `DocumentList` with `allowedCategories` set to `deliverable` and `rfp`.
- Advocacy projects:
  - Add a "Claim Details" section showing claim type, distributor company, claim amount, incident date, and damage description (display "-" when missing).
  - Add "Evidence" and "Regulatory Filing" document sections using `DocumentList` with `allowedCategories` set to `claim_evidence` and `regulatory_filing`.
- Keep efficiency-specific upload flows unchanged and avoid showing consulting/advocacy sections for efficiency.
- Add EN/ES labels for the new section titles and fields.
  </action>
  <verify>
Run:
```
rg "Consulting Overview|Deliverables|Claim Details|allowedCategories" Development/app/portal/customer/projects/[id]/page.tsx
```
Expect service-specific sections and document list filters to be present.
  </verify>
  <done>Customer project detail shows consulting or advocacy sections only for matching service types, including consulting deliverables.</done>
</task>

</tasks>

<verification>
- Consulting projects show timeline/budget/phases context for customers and a deliverables section
- Advocacy projects show claim details and evidence/regulatory document sections
- DocumentList filters to service-specific categories when requested
</verification>

<success_criteria>
- Customer portal displays service-specific project details for consulting and advocacy, including consulting deliverables
- Document lists can be constrained to service-relevant categories
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-service-features/03-04-SUMMARY.md`
</output>
