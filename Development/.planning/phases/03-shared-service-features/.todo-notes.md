# Phase 03 Todo Integration Notes

## Added Todos to Include in Phase 03 Planning

### 1. API Key Audit & Hardening (2026-02-03)
**Area:** api  
**Files:** `supabase-admin.ts`, `send-email`, `create-quote`, and other routes

**What:** Inventory all API routes and services that depend on external keys (Supabase service role, Resend), document required env vars, add guardrails with clear error messages or dev fallbacks.

**Why:** Phase 03 completion depends on stable API key handling for quote conversion and email notifications. Multiple 500 errors during testing indicate need for hardened configuration.

**Related Todos (same area):**
- Handle existing auth user on activation
- Configure Resend key or dev guard  
- Review Resend email usage

**Integration point:** Could be Wave 1 task in Phase 03 gap closure plan, or separate hardening task after verification completes.

---

### 2. Fix Global TypeScript Errors (2026-02-01)
**Area:** ui / code-quality  
**Priority:** HIGH BLOCKER  
**Files:** 
- `app/portal/admin/technicians/page.tsx` (missing setResendingId state)
- `app/services/efficiency/contratar/ContratarFlow.tsx` (duplicate object keys)
- `app/portal/admin/users/[id]/page.tsx` (type mismatch: string | undefined)
- `components/DocumentList.tsx` (type mismatch: string | undefined)
- `lib/services/project-service.ts` (unknown property pending_inspection)
- API route type constraints

**What:** Fix 19 TypeScript errors to make `npx tsc --noEmit` pass cleanly

**Why:** 
- Blocks production deployment (errors currently ignored in build config)
- Required for Phase 1/2/3 verification completion
- Prevents future TS compilation issues

**Errors Breakdown:**
1. Missing state setter (4 errors)
2. Duplicate object keys (7 errors)
3. Type mismatches string|undefined (2 errors)
4. Unknown property (1 error)
5. Route type constraints (2 errors)
6. Missing translations property (1 error)

**Integration point:** 
- Could be separate "Code Quality" phase/plan (30-45 min effort)
- Or bundled with Phase 04+ as prerequisite
- Recommend as Phase 3.1 (urgent hardening) or Phase 04-01 (first task)

**Scope:** Type correctness only - no behavior changes

