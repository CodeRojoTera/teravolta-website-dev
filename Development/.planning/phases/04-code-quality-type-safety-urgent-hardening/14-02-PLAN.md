---
phase: 14
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Development/app/portal/admin/users/[id]/page.tsx
  - Development/components/DocumentList.tsx
autonomous: true

must_haves:
  truths:
    - "users/[id]/page.tsx compiles without 'string | undefined not assignable to string' error at line 283"
    - "DocumentList.tsx compiles without 'string | undefined not assignable to string' error at line 266"
  artifacts:
    - path: "Development/app/portal/admin/users/[id]/page.tsx"
      provides: "Early-return guard for optional technicianData.id"
      contains: "if (!technicianData?.id) return;"
    - path: "Development/components/DocumentList.tsx"
      provides: "Truthy guard on doc.id before passing to handleDelete"
      contains: "doc.id && handleDelete(doc.id)"
  key_links:
    - from: "users/[id]/page.tsx line 283"
      to: "TechnicianService.update() first argument"
      via: "technicianData.id (typed string | undefined) passed where string expected"
      pattern: "TechnicianService\\.update"
    - from: "DocumentList.tsx line 266"
      to: "handleDelete function signature"
      via: "doc.id (typed string | undefined) passed where string expected"
      pattern: "handleDelete"
---

<objective>
Fix Error Group C (optional `technicianData.id` passed to TechnicianService.update) and Error Group E (optional `doc.id` passed to handleDelete). Both are `string | undefined` → `string` type mismatches caused by optional `id` fields on their respective types.

Purpose: Eliminates 2 TypeScript errors (1 in users/[id]/page.tsx at line 283, 1 in DocumentList.tsx at line 266). Both are resolved by adding narrowing guards before the function calls.
Output: Both files compile cleanly for these errors.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-code-quality-type-safety-urgent-hardening/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard optional technicianData.id before TechnicianService.update call</name>
  <files>Development/app/portal/admin/users/[id]/page.tsx</files>
  <action>
    Add an early-return guard for the optional `id` field on `technicianData`.

    The function `handleSaveTechnician` currently starts like this (lines 278-283):
        const handleSaveTechnician = async () => {
            if (!technicianData) return;
            setUpdating(true);
            try {
                // Update technicians table
                await TechnicianService.update(technicianData.id, {

    The guard at line 279 (`if (!technicianData) return;`) confirms technicianData exists, but the Technician type has `id?: string` (optional). TechnicianService.update expects `string` as first arg.

    Insert a new guard AFTER line 279 (`if (!technicianData) return;`):
        if (!technicianData.id) return;

    Result:
        const handleSaveTechnician = async () => {
            if (!technicianData) return;
            if (!technicianData.id) return;
            setUpdating(true);

    No other changes to this file.
  </action>
  <verify>Run: npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep "users/\[id\]/page.tsx" | grep "283" — output must be empty.</verify>
  <done>The argument-type mismatch error at line 283 (or shifted line after Plan 01 adds 1 line) is resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Guard optional doc.id before handleDelete call in DocumentList</name>
  <files>Development/components/DocumentList.tsx</files>
  <action>
    Add a truthy guard on `doc.id` in the onClick handler at line 266.

    Current code (line 266):
                                        onClick={() => handleDelete(doc.id)}

    Change to:
                                        onClick={() => doc.id && handleDelete(doc.id)}

    This is an inline guard: if doc.id is undefined, the short-circuit prevents handleDelete from being called. The Document type in lib/types.ts has `id?: string`, so this narrowing is required.

    No other changes to this file.
  </action>
  <verify>Run: npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep "DocumentList.tsx" — output must be empty (zero matches).</verify>
  <done>The `string | undefined` argument error at DocumentList.tsx line 266 is resolved.</done>
</task>

</tasks>

<verification>
After both tasks complete, run:
  npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep -E "DocumentList|technicianData\.id"
Expected: zero output lines.
</verification>

<success_criteria>
- `handleSaveTechnician` has `if (!technicianData.id) return;` guard after the existing `if (!technicianData) return;`
- DocumentList onClick uses `doc.id && handleDelete(doc.id)` pattern
- Both files produce zero TypeScript errors for these locations
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality-type-safety-urgent-hardening/14-02-SUMMARY.md`
</output>
