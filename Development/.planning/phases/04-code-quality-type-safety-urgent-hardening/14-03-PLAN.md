---
phase: 14
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Development/app/services/efficiency/contratar/ContratarFlow.tsx
  - Development/lib/services/project-service.ts
autonomous: true

must_haves:
  truths:
    - "ContratarFlow.tsx compiles without duplicate-key errors (7 errors at lines 371-377 eliminated)"
    - "project-service.ts compiles without 'pending_inspection' unknown-property error at line 181"
  artifacts:
    - path: "Development/app/services/efficiency/contratar/ContratarFlow.tsx"
      provides: "Object literal with single occurrence of each key (service, package, status, paymentStatus, assignedTo, scheduledDate, scheduledTime)"
      contains: "scheduledTime: formData.installationTime,"
    - path: "Development/lib/services/project-service.ts"
      provides: "Efficiency progress map without pending_inspection"
      contains: "pending_installation: 30,"
  key_links:
    - from: "ContratarFlow.tsx lines 364-370"
      to: "Project creation object literal"
      via: "First (valid) occurrence of the 7 keys kept; duplicate block at 371-377 removed"
      pattern: "scheduledTime"
    - from: "project-service.ts efficiency progress map"
      to: "EfficiencyStatus type"
      via: "pending_inspection is not a member of EfficiencyStatus — inspections tracked via appointments"
      pattern: "pending_inspection"
---

<objective>
Fix Error Group D (7 duplicate object keys in ContratarFlow.tsx) and Error Group F (invalid `pending_inspection` status in project-service.ts efficiency progress map).

Purpose: Eliminates 8 TypeScript errors (7 duplicate-key warnings in ContratarFlow, 1 unknown-property in project-service). Both are dead/duplicated code that must be deleted — no new code added.
Output: Clean object literals in both files; zero errors from these locations.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-code-quality-type-safety-urgent-hardening/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate object keys from ContratarFlow project creation literal</name>
  <files>Development/app/services/efficiency/contratar/ContratarFlow.tsx</files>
  <action>
    Delete the 7 duplicate keys at lines 371-377. These are an exact copy of the keys at lines 364-370 and must be removed entirely.

    The file currently has (lines 364-378):
        service: 'efficiency',
        package: 'Standard',
        status: 'pending_assignment',
        paymentStatus: 'paid',
        assignedTo: [],
        scheduledDate: formData.installationDate,
        scheduledTime: formData.installationTime,
        service: 'efficiency',              // <-- DELETE THIS LINE (371)
        package: 'Standard',                // <-- DELETE THIS LINE (372)
        status: 'pending_assignment',       // <-- DELETE THIS LINE (373)
        paymentStatus: 'paid',              // <-- DELETE THIS LINE (374)
        assignedTo: [],                     // <-- DELETE THIS LINE (375)
        scheduledDate: formData.installationDate,  // <-- DELETE THIS LINE (376)
        scheduledTime: formData.installationTime,  // <-- DELETE THIS LINE (377)
        invoiceSentAt: undefined,

    After deletion, lines 364-370 (the first occurrence) remain, followed immediately by:
        invoiceSentAt: undefined,

    Delete ONLY lines 371-377 (the second block). Preserve all surrounding code exactly.
  </action>
  <verify>Run: npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep "ContratarFlow.tsx" — output must be empty (zero matches).</verify>
  <done>All 7 duplicate-key errors in ContratarFlow.tsx are eliminated.</done>
</task>

<task type="auto">
  <name>Task 2: Remove invalid pending_inspection from efficiency progress map</name>
  <files>Development/lib/services/project-service.ts</files>
  <action>
    Remove the `pending_inspection: 28,` line from the efficiency section of the progress map in `calculateProgressFromStatus`.

    Current code (lines 180-182):
        pending_assignment: 25,
        pending_inspection: 28,
        pending_installation: 30,

    Delete the middle line. Result:
        pending_assignment: 25,
        pending_installation: 30,

    Context: `pending_inspection` is NOT a valid member of the `EfficiencyStatus` type. Inspections are tracked via appointments, not as a project status. This is the only change in this file.
  </action>
  <verify>Run: npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep "project-service.ts" — output must be empty (zero matches).</verify>
  <done>The unknown-property error for `pending_inspection` at line 181 is eliminated.</done>
</task>

</tasks>

<verification>
After both tasks complete, run:
  npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep -E "ContratarFlow|project-service"
Expected: zero output lines.
</verification>

<success_criteria>
- ContratarFlow.tsx object literal has exactly one occurrence of each key (service, package, status, paymentStatus, assignedTo, scheduledDate, scheduledTime)
- project-service.ts efficiency progress map has no `pending_inspection` entry
- Both files produce zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality-type-safety-urgent-hardening/14-03-SUMMARY.md`
</output>
