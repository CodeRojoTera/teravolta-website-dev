---
phase: 14
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - Development/app/api/send-email/route.ts
  - Development/app/api/notify-existing-client/route.ts
  - Development/app/api/send-invoice/route.ts
  - Development/app/api/send-onboarding-email/route.ts
  - Development/docs/API_KEYS.md
autonomous: true

must_haves:
  truths:
    - "All 4 Resend routes return 200 with skipped:true in dev when RESEND_API_KEY is unset"
    - "All 4 Resend routes return 503 in production when RESEND_API_KEY is unset"
    - "API_KEYS.md documents every env var with purpose, source, and required-per-env status"
  artifacts:
    - path: "Development/app/api/send-email/route.ts"
      provides: "Dev-safe guard replacing the existing partial guard"
      contains: "skipped: true"
    - path: "Development/app/api/notify-existing-client/route.ts"
      provides: "Dev-safe guard at top of POST handler"
      contains: "skipped: true"
    - path: "Development/app/api/send-invoice/route.ts"
      provides: "Dev-safe guard at top of POST handler"
      contains: "skipped: true"
    - path: "Development/app/api/send-onboarding-email/route.ts"
      provides: "Dev-safe guard at top of POST handler"
      contains: "skipped: true"
    - path: "Development/docs/API_KEYS.md"
      provides: "Authoritative env-var reference"
      contains: "RESEND_API_KEY"
  key_links:
    - from: "Each Resend route POST handler entry"
      to: "process.env.RESEND_API_KEY check"
      via: "Guard runs BEFORE any business logic or resend.emails.send call"
      pattern: "if \\(!process\\.env\\.RESEND_API_KEY\\)"
    - from: "API_KEYS.md"
      to: ".env.local / production env"
      via: "Documents where each key is set and how to obtain it"
      pattern: "RESEND_API_KEY"
---

<objective>
Harden all 4 Resend email routes with a consistent dev-safe guard pattern, then create the API_KEYS.md documentation. The guard allows email sends to be silently skipped in development when RESEND_API_KEY is missing, while failing loudly (503) in production.

Purpose: Addresses CONFIG-01 (dev-safe guards) and CONFIG-02 (API key documentation). Prevents hard crashes during local development and provides the team with a single reference for all environment variables.
Output: 4 hardened route files + 1 new documentation file.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-code-quality-type-safety-urgent-hardening/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply dev-safe RESEND_API_KEY guard to all 4 email routes</name>
  <files>
    Development/app/api/send-email/route.ts
    Development/app/api/notify-existing-client/route.ts
    Development/app/api/send-invoice/route.ts
    Development/app/api/send-onboarding-email/route.ts
  </files>
  <action>
    Apply the following guard block to each of the 4 files. The guard must be the FIRST thing inside the `try {` block of the POST handler — before any `request.json()` destructuring or business logic.

    Guard block to insert:
    ```typescript
        if (!process.env.RESEND_API_KEY) {
            if (process.env.NODE_ENV !== 'production') {
                console.warn('[DEV] RESEND_API_KEY missing — skipping email send');
                return NextResponse.json({ success: true, messageId: 'dev-skip', skipped: true }, { status: 200 });
            }
            return NextResponse.json({ error: 'Email service not configured' }, { status: 503 });
        }
    ```

    **File-by-file instructions:**

    === 1. Development/app/api/send-email/route.ts ===
    This file already has a PARTIAL guard at lines 11-16:
        if (!process.env.RESEND_API_KEY) {
            console.error('RESEND_API_KEY is missing');
            return NextResponse.json(
                { error: 'Server misconfiguration: Missing Email API Key' },
                { status: 500 }
            );
        }
    REPLACE that entire block (lines 11-16) with the new guard block above.
    The guard should remain AFTER `const { to, subject, html, text, from } = await request.json();` (line 9) — that's fine, the destructure doesn't touch Resend.

    === 2. Development/app/api/notify-existing-client/route.ts ===
    No guard exists. Insert the guard block as the FIRST statement inside `try {`, BEFORE line 8 (`const { to, fullName, ... } = await request.json();`).

    === 3. Development/app/api/send-invoice/route.ts ===
    No guard exists. Insert the guard block as the FIRST statement inside `try {`, BEFORE line 9 (`const { to, fullName, ... } = await request.json();`).

    === 4. Development/app/api/send-onboarding-email/route.ts ===
    No guard exists. Insert the guard block as the FIRST statement inside `try {`, BEFORE line 10 (`const { to, fullName, ... } = await request.json();`).

    IMPORTANT: Keep the module-level `const resend = new Resend(process.env.RESEND_API_KEY);` in all files. The Resend SDK handles undefined gracefully at construction time; the guard above prevents `.emails.send()` from ever executing without a valid key.

    Do NOT modify any other logic in these files.
  </action>
  <verify>
    For each file, grep for the guard pattern:
      grep -l "skipped: true" Development/app/api/send-email/route.ts Development/app/api/notify-existing-client/route.ts Development/app/api/send-invoice/route.ts Development/app/api/send-onboarding-email/route.ts
    All 4 files must appear in the output.
    Also confirm the old partial guard is gone:
      grep -c "Server misconfiguration" Development/app/api/send-email/route.ts
    Must output 0.
  </verify>
  <done>All 4 routes have the consistent dev-safe guard. The old partial guard in send-email is removed. No other code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Create API_KEYS.md environment variable documentation</name>
  <files>Development/docs/API_KEYS.md</files>
  <action>
    Create Development/docs/API_KEYS.md documenting ALL environment variables used by the TeraVolta platform.

    The file must contain:

    ```markdown
    # API Keys & Environment Variables

    Reference for all environment variables required by the TeraVolta platform.

    ## Supabase

    | Variable | Purpose | Required (Dev) | Required (Prod) | Source |
    |----------|---------|----------------|-----------------|--------|
    | NEXT_PUBLIC_SUPABASE_URL | Supabase project URL — used by client and server | Yes | Yes | Supabase Dashboard → Settings → API |
    | NEXT_PUBLIC_SUPABASE_ANON_KEY | Supabase anonymous (public) key — used by client-side queries with RLS | Yes | Yes | Supabase Dashboard → Settings → API |
    | SUPABASE_SERVICE_ROLE_KEY | Supabase service-role key — bypasses RLS, used by server-side admin operations | Yes | Yes | Supabase Dashboard → Settings → API |

    > **Security:** `SUPABASE_SERVICE_ROLE_KEY` must NEVER be exposed to the client. It is imported only in server-side files (e.g., `lib/supabase-admin.ts`).

    ## Email (Resend)

    | Variable | Purpose | Required (Dev) | Required (Prod) | Source |
    |----------|---------|----------------|-----------------|--------|
    | RESEND_API_KEY | Resend email API key — used by all `/api/send-*` and `/api/notify-*` routes | No (skipped with warning) | Yes | [Resend Dashboard](https://resend.com/dashboard) → API Keys |

    > **Dev behavior:** When `RESEND_API_KEY` is missing in a non-production environment, all email routes return `{ success: true, skipped: true }` with a `[DEV]` console warning instead of crashing. In production, missing key returns HTTP 503.

    ### Routes using Resend
    - `POST /api/send-email` — Generic outbound email
    - `POST /api/notify-existing-client` — Project-initiated client notification
    - `POST /api/send-invoice` — Invoice delivery with document links
    - `POST /api/send-onboarding-email` — Account activation / welcome email (customers + technicians)

    ## Application

    | Variable | Purpose | Required (Dev) | Required (Prod) | Source |
    |----------|---------|----------------|-----------------|--------|
    | NEXT_PUBLIC_APP_URL | Base URL for the application — used to construct magic links and portal URLs in emails | No (defaults to `http://localhost:3000`) | Yes | Set to your production domain (e.g., `https://app.teravolta.com`) |

    ## Setup Checklist

    ### Local Development (`.env.local`)
    1. Copy `.env.local.example` to `.env.local` (if example exists)
    2. Set `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` from your Supabase project
    3. Set `SUPABASE_SERVICE_ROLE_KEY` from Supabase (keep secret)
    4. Optionally set `RESEND_API_KEY` to test email sending; omit to use dev-skip mode
    5. `NEXT_PUBLIC_APP_URL` defaults to `http://localhost:3000` — override if needed

    ### Production
    All variables marked **Required (Prod) = Yes** MUST be set in your hosting environment (e.g., Vercel environment variables).
    ```

    Write this content exactly to Development/docs/API_KEYS.md.
  </action>
  <verify>
    Confirm file exists and has key sections:
      grep -c "RESEND_API_KEY" Development/docs/API_KEYS.md  → must be >= 3
      grep -c "SUPABASE_SERVICE_ROLE_KEY" Development/docs/API_KEYS.md  → must be >= 2
      grep -c "skipped" Development/docs/API_KEYS.md  → must be >= 1
      grep -c "503" Development/docs/API_KEYS.md  → must be >= 1
  </verify>
  <done>API_KEYS.md exists with complete documentation of all 5 env vars, dev vs prod requirements, and the Resend dev-skip behavior.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Confirm guard in all 4 routes: grep -rl "skipped: true" Development/app/api/ | wc -l → must be 4
2. Confirm old guard removed: grep -r "Server misconfiguration" Development/app/api/ → zero output
3. Confirm docs exist: cat Development/docs/API_KEYS.md | head -5 → shows markdown header
4. Run: npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep -E "send-email|notify-existing|send-invoice|send-onboarding" → zero output (no new TS errors introduced)
</verification>

<success_criteria>
- All 4 Resend routes have the identical dev-safe guard as their first handler logic
- send-email/route.ts no longer has the old partial guard (status 500 / "Server misconfiguration")
- API_KEYS.md documents: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, RESEND_API_KEY, NEXT_PUBLIC_APP_URL
- Each env var entry includes: purpose, dev-required status, prod-required status, and source/location
- Resend dev-skip behavior is documented
- No new TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality-type-safety-urgent-hardening/14-05-SUMMARY.md`
</output>
