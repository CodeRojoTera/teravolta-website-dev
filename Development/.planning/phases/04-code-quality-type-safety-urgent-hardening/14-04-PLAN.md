---
phase: 14
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Development/app/api/admin/projects/[id]/status/route.ts
  - Development/app/api/projects/[id]/status/route.ts
autonomous: true

must_haves:
  truths:
    - "Both status route files compile without Next.js 15 async-params type errors"
    - "All three handlers (admin PATCH, admin GET, non-admin PATCH) use awaited params"
  artifacts:
    - path: "Development/app/api/admin/projects/[id]/status/route.ts"
      provides: "PATCH and GET handlers with Promise<{id: string}> params signature"
      contains: "const { id } = await params;"
    - path: "Development/app/api/projects/[id]/status/route.ts"
      provides: "PATCH handler with Promise<{id: string}> params signature"
      contains: "const { id } = await params;"
  key_links:
    - from: "admin status route PATCH (line 55)"
      to: "params.id usage throughout handler"
      via: "params type changed to Promise, destructured after await"
      pattern: "await params"
    - from: "admin status route GET (line 121)"
      to: "params.id usage throughout handler"
      via: "params type changed to Promise, destructured after await"
      pattern: "await params"
    - from: "non-admin status route PATCH (line 55)"
      to: "params.id usage throughout handler"
      via: "params type changed to Promise, destructured after await"
      pattern: "await params"
---

<objective>
Fix Error Group G: Next.js 15 requires dynamic route segment params to be `Promise<{id: string}>` (not the old synchronous `{id: string}`). Three handlers across two files use the legacy signature and must be migrated.

Purpose: Eliminates 2 TypeScript errors from generated `.next/types/` (root cause in these two source files). Next.js 15 enforces async params at the type level; the old sync destructure pattern produces type mismatches at compile time.
Output: All three handlers correctly `await` their params before use.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-code-quality-type-safety-urgent-hardening/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate admin status route to Next.js 15 async params (PATCH + GET)</name>
  <files>Development/app/api/admin/projects/[id]/status/route.ts</files>
  <action>
    Two handlers in this file need the same treatment. Apply to BOTH:

    === PATCH handler (starts at line 55) ===

    Current signature (lines 55-57):
        export async function PATCH(
          request: Request,
          { params }: { params: { id: string } }
        ) {

    Change to:
        export async function PATCH(
          request: Request,
          { params }: { params: Promise<{ id: string }> }
        ) {

    Current first line of the try block (line 60):
            const projectId = params.id;

    Change to:
            const { id } = await params;
            const projectId = id;

    (Or simply: `const projectId = (await params).id;` — either form is fine.)

    No other references to `params.id` exist in this handler — `projectId` is used throughout the rest.

    === GET handler (starts at line 121) ===

    Current signature (lines 121-123):
        export async function GET(
          request: Request,
          { params }: { params: { id: string } }
        ) {

    Change to:
        export async function GET(
          request: Request,
          { params }: { params: Promise<{ id: string }> }
        ) {

    Current first line of the try block (line 126):
            const projectId = params.id;

    Change to:
            const { id } = await params;
            const projectId = id;

    No other `params.id` references in this handler either.
  </action>
  <verify>Run: npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep "admin/projects/\[id\]/status" — output must be empty.</verify>
  <done>Both PATCH and GET in the admin status route compile cleanly with async params.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate non-admin status route PATCH to Next.js 15 async params</name>
  <files>Development/app/api/projects/[id]/status/route.ts</files>
  <action>
    This file has only a PATCH handler (no GET). Apply the same pattern:

    Current signature (lines 55-57):
        export async function PATCH(
          request: Request,
          { params }: { params: { id: string } }
        ) {

    Change to:
        export async function PATCH(
          request: Request,
          { params }: { params: Promise<{ id: string }> }
        ) {

    Current first line of the try block (line 60):
            const projectId = params.id;

    Change to:
            const { id } = await params;
            const projectId = id;

    Scan the rest of the handler for any other `params.id` references and replace with `id` (or `projectId` if it's already assigned to that variable). The pattern is identical to the admin route.
  </action>
  <verify>Run: npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep "api/projects/\[id\]/status" — output must be empty.</verify>
  <done>The non-admin PATCH handler compiles cleanly with async params.</done>
</task>

</tasks>

<verification>
After both tasks complete, run:
  npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep -i "status/route"
Expected: zero output lines. Also verify no runtime regression by checking the file compiles with no syntax errors:
  npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep -c "error TS"
This count should drop by at least 2 compared to before this plan ran.
</verification>

<success_criteria>
- Both status route files use `{ params: Promise<{ id: string }> }` in all handler signatures
- All handlers `await params` before accessing `id`
- No `params.id` references remain (all use the destructured `id` or derived `projectId`)
- Zero TypeScript errors from either file
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality-type-safety-urgent-hardening/14-04-SUMMARY.md`
</output>
