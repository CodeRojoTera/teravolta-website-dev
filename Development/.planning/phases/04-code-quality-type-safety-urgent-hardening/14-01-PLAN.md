---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Development/app/portal/admin/technicians/page.tsx
  - Development/app/portal/admin/users/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "technicians/page.tsx compiles without 'resendingId' / 'setResendingId' not-defined errors"
    - "users/[id]/page.tsx compiles without 't.profile' property-missing error at line 582"
  artifacts:
    - path: "Development/app/portal/admin/technicians/page.tsx"
      provides: "useState declaration for resendingId"
      contains: "const [resendingId, setResendingId] = useState"
    - path: "Development/app/portal/admin/users/[id]/page.tsx"
      provides: "profile translation key in t object"
      contains: "profile:"
  key_links:
    - from: "technicians/page.tsx line 64"
      to: "useState declaration"
      via: "resendingId state variable used in handleResendInvite"
      pattern: "setResendingId"
    - from: "users/[id]/page.tsx line 582"
      to: "t object definition (lines 170-200)"
      via: "t.profile used in JSX heading"
      pattern: "t\\.profile"
---

<objective>
Fix Error Group A (missing useState for resendingId in technicians page) and Error Group B (missing translation key `profile` in users detail page).

Purpose: Eliminates 5 TypeScript errors (4 in technicians/page.tsx, 1 in users/[id]/page.tsx). Both are declaration-level omissions — variables/keys used but never defined.
Output: Two source files with complete declarations; zero errors from these files on `tsc --noEmit`.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-code-quality-type-safety-urgent-hardening/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing resendingId useState to technicians page</name>
  <files>Development/app/portal/admin/technicians/page.tsx</files>
  <action>
    Add the missing useState declaration for `resendingId` and `setResendingId`.

    After line 23 which reads:
        const [searchQuery, setSearchQuery] = useState('');

    Insert:
        const [resendingId, setResendingId] = useState<string | null>(null);

    That is the ONLY change in this file. Do NOT modify any other lines.

    Context: `setResendingId(tech.id || '')` is called at line 64 and `setResendingId(null)` at line 84. The state variable `resendingId` is also referenced at lines 206 and 210. All 4 errors resolve once the declaration exists.
  </action>
  <verify>Run: npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep -c "technicians/page.tsx" — output must be 0.</verify>
  <done>Zero TypeScript errors referencing technicians/page.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Add missing `profile` translation key to users detail page</name>
  <files>Development/app/portal/admin/users/[id]/page.tsx</files>
  <action>
    Add the missing `profile` key to the `t` translation object.

    Find the line that reads (line 199):
        onlySuperCanEdit: language === 'es' ? 'Solo Super Admin puede editar' : 'Only Super Admin can edit',

    That line is currently the LAST key before the closing `};` of the `t` object (line 200).
    Add a new key AFTER `onlySuperCanEdit`, before the closing `};`:

        profile: language === 'es' ? 'Perfil' : 'Profile',

    Result should look like:
        onlySuperCanEdit: language === 'es' ? 'Solo Super Admin puede editar' : 'Only Super Admin can edit',
        profile: language === 'es' ? 'Perfil' : 'Profile',
    };

    Context: `t.profile` is used at line 582 in the technician profile editor section heading. This is the only missing key — no other changes to this file in this task.
  </action>
  <verify>Run: npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep "users/\[id\]/page.tsx" | grep "profile" — output must be empty (zero matches).</verify>
  <done>The `t.profile` property-missing error at line 582 is resolved.</done>
</task>

</tasks>

<verification>
After both tasks complete, run:
  npx tsc --noEmit -p ./Development/tsconfig.json 2>&1 | grep -E "technicians/page|users/\[id\]/page" | grep -v "technicianData"
Expected: zero output lines (the only remaining users/[id] error is the technicianData.id guard, handled in Plan 02).
</verification>

<success_criteria>
- `resendingId` and `setResendingId` are declared as useState in technicians/page.tsx
- `profile` key exists in the `t` object in users/[id]/page.tsx
- No TypeScript errors originate from either file for the declarations added here
</success_criteria>

<output>
After completion, create `.planning/phases/04-code-quality-type-safety-urgent-hardening/14-01-SUMMARY.md`
</output>
