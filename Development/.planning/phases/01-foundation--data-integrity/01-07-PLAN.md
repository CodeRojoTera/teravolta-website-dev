---
phase: 01-foundation--data-integrity
plan: 07
type: execute
wave: 2
depends_on:
  - 01-10
files_modified:
  - Development/components/ManualProjectWizard.tsx
  - Development/components/NotificationContext.tsx
  - Development/components/RoleGuard.tsx
  - Development/components/AuthProvider.tsx
  - Development/components/Header.tsx
  - Development/lib/clientTypeUtils.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Shared components and utilities use active_users so soft-deleted users never appear in shared UI logic"
  artifacts:
    - path: "supabase/migrations/20260129000002_create_active_users_view.sql"
      provides: "active_users view definition used for read queries"
    - path: "Development/components/AuthProvider.tsx"
      provides: "User profile lookups read from active_users"
    - path: "Development/lib/clientTypeUtils.ts"
      provides: "User role/type lookups read from active_users"
  key_links:
    - from: "Development/components/AuthProvider.tsx"
      to: "active_users"
      via: "supabase.from('active_users')"
      pattern: "from\('active_users'\)"
    - from: "Development/lib/clientTypeUtils.ts"
      to: "active_users"
      via: "supabase.from('active_users')"
      pattern: "from\('active_users'\)"
---

<objective>
Ensure shared components and utilities use the active_users view so soft-deleted users never appear in shared UI logic.

Purpose: Enforce soft-delete filtering in shared UI logic and helper utilities.
Output: Shared components/utilities read from active_users while writes remain on users.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation--data-integrity/01-01-SUMMARY.md
@supabase/migrations/20260129000002_create_active_users_view.sql
@Development/components/AuthProvider.tsx
@Development/app/api/create-project/route.ts
@Development/app/services/technicianService.ts
@Development/lib/clientTypeUtils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shared components and utilities to read from active_users</name>
  <files>
Development/components/AuthProvider.tsx, Development/components/Header.tsx, Development/components/NotificationContext.tsx, Development/components/RoleGuard.tsx, Development/components/ManualProjectWizard.tsx, Development/lib/clientTypeUtils.ts
  </files>
  <action>
Audit each `.from('users')` read and replace with `.from('active_users')` for SELECT-only lookups.
- Keep INSERT/UPDATE/DELETE operations on the base users table.
- If a query combines read + write, split into a read from active_users and a write to users.
- Preserve existing filters and ordering; only change the source table for reads.
  </action>
  <verify>Run: `rg "from\('users'\)" "Development/components" "Development/lib/clientTypeUtils.ts"` and confirm remaining matches are writes only</verify>
  <done>Shared components/utilities read users via active_users and no soft-deleted users render in shared UI</done>
  </task>

</tasks>

<verification>
- Shared components no longer render soft-deleted users.
</verification>

<success_criteria>
- active_users is the source for user read queries across shared components and utilities.
- No shared component or utility response returns soft-deleted users.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation--data-integrity/01-07-SUMMARY.md`
</output>
