---
phase: 01-foundation--data-integrity
plan: 13
type: execute
wave: 2
depends_on:
  - 01-10
files_modified:
  - Development/app/services/technicianService.ts
  - Development/app/api/admin/projects/[id]/status/route.ts
  - Development/app/api/create-project/route.ts
  - Development/app/api/update-user-password/route.ts
  - Development/app/api/activate-account/route.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "API/service user lookups use active_users and exclude soft-deleted users"
  artifacts:
    - path: "Development/app/api/create-project/route.ts"
      provides: "API user lookups read from active_users"
    - path: "Development/app/services/technicianService.ts"
      provides: "Technician service user lookups read from active_users"
  key_links:
    - from: "Development/app/api/create-project/route.ts"
      to: "active_users"
      via: "supabase.from('active_users')"
      pattern: "from\('active_users'\)"
    - from: "Development/app/services/technicianService.ts"
      to: "active_users"
      via: "supabase.from('active_users')"
      pattern: "from\('active_users'\)"
---

<objective>
Switch API and service user reads to active_users so soft-deleted users never appear in API responses.

Purpose: Enforce soft-delete filtering for backend lookups that power UI and automation.
Output: API/service reads use active_users while writes remain on users.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/20260129000002_create_active_users_view.sql
@Development/app/services/technicianService.ts
@Development/app/api/create-project/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API and services to read from active_users</name>
  <files>
Development/app/services/technicianService.ts, Development/app/api/admin/projects/[id]/status/route.ts, Development/app/api/create-project/route.ts, Development/app/api/update-user-password/route.ts, Development/app/api/activate-account/route.ts
  </files>
  <action>
Replace any SELECT-only lookups against users with active_users.
- Keep writes (account activation, password updates, user mutations) on users.
- If a handler mixes read and write, split into a read from active_users and a write to users to avoid losing soft-deleted filtering.
  </action>
  <verify>Run: `rg "from\('users'\)" "Development/app/api" "Development/app/services/technicianService.ts"` and confirm remaining matches are writes only</verify>
  <done>API/service reads use active_users while write paths continue to target users</done>
</task>

</tasks>

<verification>
- API/service user lookups only return active users.
</verification>

<success_criteria>
- active_users is the source for user read queries across APIs/services.
- No API response returns soft-deleted users.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation--data-integrity/01-13-SUMMARY.md`
</output>
