---
phase: 01-foundation--data-integrity
plan: 06
type: execute
wave: 2
depends_on:
  - 01-04
  - 01-10
files_modified:
  - Development/lib/services/user-deletion.ts
  - Development/lib/services/project-service.ts
  - Development/app/portal/admin/users/clients/page.tsx
  - Development/app/portal/customer/settings/page.tsx
  - Development/app/portal/admin/requests/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Admin can schedule or cancel user deletion within the 15-day grace period"
    - "Customer deletion requests use the user-deletion service (no legacy deletion_requests flow)"
    - "Project status changes triggered by user deletion go through the state machine"
  artifacts:
    - path: "Development/lib/services/user-deletion.ts"
      provides: "Schedule/cancel/execute deletion workflows with state-machine status updates"
    - path: "Development/lib/services/project-service.ts"
      provides: "updateProjectStatus API used by deletion workflows"
    - path: "Development/app/portal/admin/users/clients/page.tsx"
      provides: "Admin UI actions wired to schedule/cancel/execute user deletion"
    - path: "Development/app/portal/customer/settings/page.tsx"
      provides: "Customer self-service deletion scheduling and cancellation"
    - path: "Development/app/portal/admin/requests/page.tsx"
      provides: "Admin request center without deletion_requests dependency"
  key_links:
    - from: "Development/app/portal/admin/users/clients/page.tsx"
      to: "Development/lib/services/user-deletion.ts"
      via: "scheduleUserDeletion/cancelScheduledDeletion/executeHardDelete"
      pattern: "scheduleUserDeletion|cancelScheduledDeletion|executeHardDelete"
    - from: "Development/app/portal/customer/settings/page.tsx"
      to: "Development/lib/services/user-deletion.ts"
      via: "scheduleUserDeletion/cancelScheduledDeletion"
      pattern: "scheduleUserDeletion|cancelScheduledDeletion"
    - from: "Development/lib/services/user-deletion.ts"
      to: "Development/lib/services/project-service.ts"
      via: "updateProjectStatus"
      pattern: "updateProjectStatus"
---

<objective>
Wire admin and customer deletion flows to the new user-deletion service, remove legacy deletion_requests usage, and ensure deletion-driven project status updates are validated by the state machine.

Purpose: Restore the 15-day recovery workflow and prevent bypassing state-machine rules during deletions.
Output: Updated deletion UI/actions, cleaned request center, and state-machine-safe deletion logic.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation--data-integrity/01-02-SUMMARY.md
@.planning/phases/01-foundation--data-integrity/01-04-SUMMARY.md
@Development/lib/services/user-deletion.ts
@Development/lib/services/project-service.ts
@Development/app/portal/admin/users/clients/page.tsx
@Development/app/portal/customer/settings/page.tsx
@Development/app/portal/admin/requests/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route deletion-driven project status changes through state machine</name>
  <files>Development/lib/services/user-deletion.ts, Development/lib/services/project-service.ts</files>
  <action>
Replace direct updates to active_projects status inside user deletion flows with updateProjectStatus calls.
- In scheduleUserDeletion and executeSoftDelete, fetch affected active project IDs and call updateProjectStatus for each with the same target status used today (do not invent new statuses).
- Pass isAdmin=true and requestedBy (or system) as the actor; include notes about deletion scheduling/soft delete for audit clarity.
- Keep database writes to users and deletion audit logic unchanged; only change the project status update path.
  </action>
  <verify>Run: `npx tsc --noEmit "Development/lib/services/user-deletion.ts"`</verify>
  <done>user-deletion no longer updates project status directly; all status changes call updateProjectStatus</done>
</task>

<task type="auto">
  <name>Task 2: Wire admin client deletion actions to user-deletion service</name>
  <files>Development/app/portal/admin/users/clients/page.tsx</files>
  <action>
Replace direct Supabase delete/update flows with user-deletion service calls.
- For "Deactivate account (keep data)" use scheduleUserDeletion with reason "admin_action" and requestedBy authUser.id.
- For "Delete permanently" use executeHardDelete with reason "admin_action"; preserve current follow-up UI (toast + list updates).
- Add a cancel option when deletion_scheduled_for is present: call cancelScheduledDeletion and restore active status in UI.
- Switch role/user fetch reads to active_users for list/role lookups, but keep writes on users when required by the service.
  </action>
  <verify>Run: `rg "deletion_requests" "Development/app/portal/admin/users/clients/page.tsx"` returns no matches</verify>
  <done>Admin delete actions schedule/cancel/hard delete via user-deletion service and users list excludes soft-deleted users</done>
</task>

<task type="auto">
  <name>Task 3: Replace legacy deletion_requests in customer settings and admin request center</name>
  <files>Development/app/portal/customer/settings/page.tsx, Development/app/portal/admin/requests/page.tsx</files>
  <action>
Remove deletion_requests usage and route customer deletion through user-deletion service.
- In customer settings, replace deletion_requests fetch/insert with scheduleUserDeletion and cancelScheduledDeletion; show pending state based on deletion_scheduled_for from active_users.
- In admin requests, remove deletion_requests query/merge logic and display deletion-related entries from admin_requests only.
- Keep user_settings and other request types intact.
  </action>
  <verify>Run: `rg "deletion_requests" "Development/app/portal/customer/settings/page.tsx" "Development/app/portal/admin/requests/page.tsx"` returns no matches</verify>
  <done>Customer deletion uses schedule/cancel flows and admin request center has no dependency on deletion_requests</done>
</task>

</tasks>

<verification>
- Customer settings: deletion request shows scheduled state and can be cancelled without errors.
- Admin clients: schedule, cancel, and hard delete complete without direct users-table status edits.
</verification>

<success_criteria>
- No UI or service code references deletion_requests for account deletion.
- Deletion-driven project status changes route through updateProjectStatus.
- Admin can cancel scheduled deletions to recover users within grace period.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation--data-integrity/01-06-SUMMARY.md`
</output>
