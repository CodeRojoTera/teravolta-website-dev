---
phase: 01-foundation--data-integrity
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/state-machines/project-states.ts
  - Development/app/api/admin/projects/[id]/status/route.ts
  - Development/app/portal/admin/active-projects/[id]/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "State machine validates efficiency/consulting/advocacy status transitions"
    - "Admin status options derive from valid next transitions"
  artifacts:
    - path: "lib/state-machines/project-states.ts"
      provides: "Service-specific statuses and transition guards"
    - path: "Development/app/api/admin/projects/[id]/status/route.ts"
      provides: "Valid next status API"
    - path: "Development/app/portal/admin/active-projects/[id]/page.tsx"
      provides: "Admin status dropdown uses valid transitions"
  key_links:
    - from: "Development/app/api/admin/projects/[id]/status/route.ts"
      to: "lib/state-machines/project-states.ts"
      via: "getValidTransitions"
      pattern: "getValidTransitions"
    - from: "Development/app/portal/admin/active-projects/[id]/page.tsx"
      to: "Development/app/api/admin/projects/[id]/status/route.ts"
      via: "GET status options"
      pattern: "status"
---

<objective>
Confirm and fix UI-01..UI-03 coverage by ensuring state-machine definitions and admin status option wiring are complete.

Purpose: Close verification gaps by enforcing state-machine rules and admin status option filtering end-to-end.
Output: Updated state-machine definitions (if needed) and verified admin status option wiring.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/state-machines/project-states.ts
@Development/app/api/admin/projects/[id]/status/route.ts
@Development/app/portal/admin/active-projects/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure state machine includes all service-specific statuses and transitions</name>
  <files>lib/state-machines/project-states.ts</files>
  <action>
Review `lib/state-machines/project-states.ts` and ensure:
- Efficiency statuses include pending_inspection/pending_installation (and any EE-specific states already in use).
- Consulting statuses include requirements_defined, rfp_preparation, offers_evaluation, supplier_selection.
- Advocacy statuses include pending_audit, claim_formulation, claim_filed, asep_filed, resolved.

If any status or transition is missing, add it with valid transitions matching the existing state-machine patterns. Keep naming consistent with existing enums and customer/admin labels.
  </action>
  <verify>Run: `npx vitest run lib/state-machines/project-states.test.ts` (or `npx vitest run` if the test path differs)</verify>
  <done>State-machine definitions include all service-specific statuses and transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure admin status options use valid transition API</name>
  <files>Development/app/api/admin/projects/[id]/status/route.ts, Development/app/portal/admin/active-projects/[id]/page.tsx</files>
  <action>
Verify and fix wiring so admin status options derive from the valid transitions API:
- Ensure the admin status API uses state-machine utilities (getValidTransitions/canTransition).
- Ensure the admin project detail page fetches options from the admin status API (GET) and uses those for the dropdown.

If the page still uses hardcoded status lists or direct updates, update it to use the API results.
  </action>
  <verify>Run: `rg "getValidTransitions|/api/admin/projects/.*/status" "Development/app/api/admin/projects/[id]/status/route.ts" "Development/app/portal/admin/active-projects/[id]/page.tsx"`</verify>
  <done>Admin status options are sourced from the valid transition API and validated by the state machine.</done>
</task>

</tasks>

<verification>
- UI-01..UI-03 enforcement confirmed through state-machine tests and admin UI wiring.
</verification>

<success_criteria>
- State machine coverage for all services is complete and tested.
- Admin status option wiring is enforced from API to UI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation--data-integrity/01-12-SUMMARY.md`
</output>
