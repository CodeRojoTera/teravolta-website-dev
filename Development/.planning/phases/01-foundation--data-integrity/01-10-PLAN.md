---
phase: 01-foundation--data-integrity
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260129000002_create_active_users_view.sql
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "active_users view exists and can be queried"
  artifacts:
    - path: "supabase/migrations/20260129000002_create_active_users_view.sql"
      provides: "active_users view definition for soft-delete filtering"
  key_links:
    - from: "supabase/migrations/20260129000002_create_active_users_view.sql"
      to: "public.active_users"
      via: "CREATE VIEW"
      pattern: "CREATE VIEW"
---

<objective>
Apply the active_users view migration so all application reads can rely on the soft-delete filtered view.

Purpose: Ensure the data source used by UI and API read queries exists before switching reads.
Output: active_users view created in the database from the existing migration.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/20260129000002_create_active_users_view.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply active_users view migration</name>
  <files>supabase/migrations/20260129000002_create_active_users_view.sql</files>
  <action>
Apply the existing migration to the target database.
- Use `npx supabase db push` (linked project) or apply via Supabase MCP if available.
- Do not modify the migration unless it fails; if it fails, fix the SQL and re-apply.
  </action>
  <verify>Run SQL: `select to_regclass('public.active_users');` returns `active_users`</verify>
  <done>active_users view exists and is queryable</done>
</task>

</tasks>

<verification>
- Querying `select count(*) from active_users;` succeeds without errors.
</verification>

<success_criteria>
- Database has active_users view available for app reads.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation--data-integrity/01-10-SUMMARY.md`
</output>
