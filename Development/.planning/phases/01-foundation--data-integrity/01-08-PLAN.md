---
phase: 01-foundation--data-integrity
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260130000004_create_deletion_audit_triggers.sql
  - supabase/migrations/20260130000021_cleanup_deletion_requests.sql
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Deletion audit log shows who deleted what, when, and why"
    - "Legacy deletion_requests removed"
  artifacts:
    - path: "supabase/migrations/20260130000004_create_deletion_audit_triggers.sql"
      provides: "Trigger functions and triggers for deletion audit logging"
    - path: "supabase/migrations/20260130000021_cleanup_deletion_requests.sql"
      provides: "Migration removing legacy deletion_requests table"
  key_links:
    - from: "supabase/migrations/20260130000004_create_deletion_audit_triggers.sql"
      to: "public.deletion_audit_log"
      via: "CREATE TRIGGER"
      pattern: "CREATE TRIGGER"
---

<objective>
Apply the audit trigger migration and remove the legacy deletion_requests table so deletes are logged and old flows are fully retired.

Purpose: Ensure deletion auditing is comprehensive and eliminate legacy cleanup debt.
Output: Triggers active in database and deletion_requests dropped.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/20260130000004_create_deletion_audit_triggers.sql
@supabase/migrations/20260130000021_cleanup_deletion_requests.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply deletion audit trigger migration</name>
  <files>supabase/migrations/20260130000004_create_deletion_audit_triggers.sql</files>
  <action>
Apply the existing audit trigger migration to the target database.
- Use `npx supabase db push` (linked project) or apply via Supabase MCP if available.
- Do not modify the migration unless it fails; if it fails, fix the SQL and re-apply.
  </action>
  <verify>Run SQL: `select tgname, tgrelid::regclass from pg_trigger where tgname ilike '%deletion%';` returns the audit triggers</verify>
  <done>Audit triggers are present and attached to the intended tables</done>
</task>

<task type="auto">
  <name>Task 2: Apply deletion_requests cleanup migration</name>
  <files>supabase/migrations/20260130000021_cleanup_deletion_requests.sql</files>
  <action>
Apply the cleanup migration that removes the legacy deletion_requests table and any related artifacts.
- Use the same migration application method as Task 1.
- If the table is already dropped, ensure migration is marked applied or re-run safely.
  </action>
  <verify>Run SQL: `select to_regclass('public.deletion_requests');` returns null</verify>
  <done>deletion_requests no longer exists in the database</done>
</task>

</tasks>

<verification>
- Deleting a test row inserts an entry into deletion_audit_log.
- deletion_requests table is absent.
</verification>

<success_criteria>
- Audit triggers are active and logging deletes.
- Legacy deletion_requests table is removed from the schema.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation--data-integrity/01-08-SUMMARY.md`
</output>
