---
phase: 01-foundation--data-integrity
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260130000020_add_soft_delete_to_documents.sql
  - Development/lib/documentUtils.ts
  - Development/components/DocumentList.tsx
  - Development/components/DocumentManager.tsx
  - Development/app/services/activeProjectService.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Documents table has soft delete support for recovery"
  artifacts:
    - path: "supabase/migrations/20260130000020_add_soft_delete_to_documents.sql"
      provides: "documents.deleted_at and deleted_by columns with indexes"
    - path: "Development/lib/documentUtils.ts"
      provides: "Document CRUD filters deleted_at and soft-deletes records"
    - path: "Development/components/DocumentList.tsx"
      provides: "Document delete action calls soft delete"
    - path: "Development/app/services/activeProjectService.ts"
      provides: "Project document queries exclude deleted_at"
  key_links:
    - from: "Development/components/DocumentList.tsx"
      to: "Development/lib/documentUtils.ts"
      via: "deleteDocument"
      pattern: "deleteDocument"
    - from: "Development/lib/documentUtils.ts"
      to: "documents"
      via: "update deleted_at/deleted_by"
      pattern: "deleted_at"
---

<objective>
Add missing soft-delete metadata for documents and update document CRUD to soft delete and filter out deleted records.

Purpose: Enable document recovery and prevent hard-delete data loss.
Output: Documents schema includes deleted_by and all document queries respect deleted_at.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/migrations/20260130000020_add_soft_delete_to_documents.sql
@Development/lib/documentUtils.ts
@Development/components/DocumentList.tsx
@Development/components/DocumentManager.tsx
@Development/app/services/activeProjectService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure documents deleted_by column and indexes exist</name>
  <files>supabase/migrations/20260130000020_add_soft_delete_to_documents.sql</files>
  <action>
Inspect the existing migration for documents soft delete support.
- If deleted_by or supporting indexes are missing, extend this existing migration to add `deleted_by` (UUID FK to users) and indexes on deleted_at/updated_at as needed. Do not create a new migration in this task.
- Apply the migration to the database using `npx supabase db push` or Supabase MCP.
  </action>
  <verify>Run SQL: `select column_name from information_schema.columns where table_name='documents' and column_name in ('deleted_at','deleted_by');` returns both columns</verify>
  <done>documents table includes deleted_at and deleted_by with indexes for filtering</done>
</task>

<task type="auto">
  <name>Task 2: Update document CRUD to soft delete and filter deleted_at</name>
  <files>Development/lib/documentUtils.ts, Development/components/DocumentList.tsx, Development/components/DocumentManager.tsx, Development/app/services/activeProjectService.ts</files>
  <action>
Implement soft delete behavior across document utilities and services.
- Update getDocumentsForEntity/getDocumentsByCategory to filter `.is('deleted_at', null)`.
- Change deleteDocument to update deleted_at/deleted_by instead of deleting the row; keep storage files intact for recovery.
- Pass current user ID into deleteDocument from DocumentList (add useAuth or prop) so deleted_by is recorded.
- Update activeProjectService document queries to exclude deleted_at records and use soft delete when removing documents.
  </action>
  <verify>Run: `rg "deleted_at" "Development/lib/documentUtils.ts" "Development/app/services/activeProjectService.ts"` and confirm filters are applied; run `rg "delete\(\)" "Development/lib/documentUtils.ts"` to ensure hard deletes removed</verify>
  <done>Document lists exclude soft-deleted rows and delete actions set deleted_at/deleted_by instead of hard delete</done>
</task>

</tasks>

<verification>
- Deleting a document hides it from lists without removing storage, and deleted_by is populated.
- Document queries in project services exclude deleted documents.
</verification>

<success_criteria>
- documents table supports soft delete with deleted_by tracking.
- App document CRUD uses soft delete and filters out deleted_at.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation--data-integrity/01-09-SUMMARY.md`
</output>
